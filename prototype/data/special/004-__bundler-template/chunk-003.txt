= id) : [...ex, id];\n      return { disputeExcludedIds: nextEx, disputeDraftDate: nextEx.length ? (s.disputeDraftDate || this.todayShort()) : null };\n    }, () => { if (this.state.prepMemoId) this.prepSyncAttachment(); });\n  };\n  closePrep = () => this.setState({ prepMemoId: null, prepSendPhase: \"idle\", prepManualChosen: false, prepCsmOptIn: false, prepManualActionTaken: false, prepManualSentDone: false });\n  prepRealGroupIds(prepIds) {\n    const P = window.CM_PKG; if (!P || !P.groups) return [];\n    return prepIds.map(gid => { const i = parseInt(String(gid).slice(1), 10); return P.groups[i] && P.groups[i].id; }).filter(Boolean);\n  }\n  markGroupsPursued = (ids, m, via) => {\n    if (!ids || !ids.length) return;\n    const now = this.nowStr();\n    const dateLabel = this.fmtDateLong(new Date().toISOString().slice(0, 10));\n    const eligibleIds = this.prepRealGroupIds(this.prepAllGroups(m).map(g => g.id));\n    let pursuedCount = 0, notPursuedCount = 0; const affected = [];\n    this.setState(s => {\n      const nextGroups = s.reviewGroups.map(g => {\n        if (!eligibleIds.includes(g.id)) return g;\n        if (ids.includes(g.id)) {\n          if (g.pursuit === \"pursued\") return g;\n          pursuedCount++; affected.push(g.id);\n          return { ...g, pursuit: \"pursued\", pursuedAt: now, pursuedBy: \"Tori Matthews\", pursuedVia: via, threePl: m.provider,\n            collection: { status: \"awaiting\", amountN: null, date: null, reason: \"\", history: (g.collection && g.collection.history) || [] } };\n        }\n        if (g.pursuit === \"pursued\") return g;\n        notPursuedCount++; affected.push(g.id);\n        return { ...g, pursuit: \"excluded\", excludedRequestDate: dateLabel };\n      });\n      const parts = [];\n      if (pursuedCount) parts.push(pursuedCount + \" variance group\" + (pursuedCount === 1 ? \"\" : \"s\") + \" marked Pursued.\");\n      if (notPursuedCount) parts.push(notPursuedCount + \" variance group\" + (notPursuedCount === 1 ? \"\" : \"s\") + \" marked Not pursued.\");\n      return { reviewGroups: nextGroups, prepUpdateBanner: parts.length ? parts.join(\" \") : null, prepUpdatedIds: affected, disputeExcludedIds: [], disputeDraftDate: null };\n    });\n    clearTimeout(this._bannerTimer);\n    this._bannerTimer = setTimeout(() => this.setState({ prepUpdateBanner: null, prepUpdatedIds: null }), 9000);\n    this.trackEvent(\"variance_groups_pursued\", { account: \"Implentio\", user: \"tori@implentio.com\", creditMemoId: m.id, version: m.version, groupIds: ids, via });\n  };\n  recordDisputePackageEvent(ids, m, via) {\n    if (!ids || !ids.length) return;\n    const groups = (this.state.reviewGroups || []).filter(g => ids.includes(g.id));\n    if (!groups.length) return;\n    const amountN = this.r2(groups.reduce((a, g) => a + (g.amountN || 0), 0));\n    const now = new Date();\n    const when = now.toLocaleString(\"en-US\", { month: \"short\", day: \"numeric\", year: \"numeric\" }) + \" at \" + now.toLocaleString(\"en-US\", { hour: \"numeric\", minute: \"2-digit\" });\n    const sel = this.prepSelection(m);\n    const acct = this.prepEmailAccount();\n    const ccParts = [this.state.prepCc || \"\"];\n    if (this.state.prepCsmOptIn) ccParts.push(this.supportRecipients().map(r => r.email).join(\", \"));\n    const ev = {\n      id: \"dpe-\" + Date.now() + \"-\" + Math.floor(Math.random() * 10000),\n      via, threePl: m.provider, memoId: m.id, version: m.version,\n      groupCount: groups.length, amount: this.fmt(amountN),\n      groups: groups.map(g => ({ title: g.title, amount: this.fmt(g.amountN || 0) })),\n      by: \"Tori Matthews\", when,\n      method: via === \"manual\" ? \"Manual (download and copy)\" : (acct ? acct.providerName : \"\\u2014\"),\n      senderEmail: via === \"manual\" ? \"tori@implentio.com\" : (acct ? acct.email : \"\\u2014\"),\n      to: this.state.prepTo || \"\\u2014\", cc: ccParts.filter(Boolean).join(\", \") || \"\\u2014\",\n      evidenceFile: this.evidenceFileName(m, sel),\n    };\n    this.setState(s => ({ disputeEvents: [ev, ...(s.disputeEvents || [])] }));\n  }\n  toggleDisputeEvent = (e) => { const id = e.currentTarget.dataset.id; this.setState(s => ({ openDispute: { ...s.openDispute, [id]: !s.openDispute[id] } })); };\n  prepGoConnect = () => this.setState({ prepManualChosen: false, prepStep: 2 });\n  confirmMarkPursued = () => {\n    const m = this.prepMemo(); if (!m) return;\n    const sel = this.prepSelection(m);\n    const realIds = this.prepRealGroupIds(sel.selGroups.map(g => g.id));\n    this.markGroupsPursued(realIds, m, \"manual\");\n    this.recordDisputePackageEvent(realIds, m, \"manual\");\n    this.setState({ prepManualActionTaken: false, prepManualSentDone: true }, () => this.showToast(\"ok\", \"Findings marked pursued with \" + m.provider + \".\"));\n  };\n  dismissMarkPursued = () => this.setState({ prepManualActionTaken: false });\n  prepMemo() { return this.memoList().find(x => x.id === this.state.prepMemoId) || null; }\n  prepAllGroups(m) {\n    if (this.state.findingsUnavailable) return [];\n    const P = window.CM_PKG;\n    if (!(m && m.golden && P && P.groups)) return [];\n    const pursuedReal = new Set((this.state.reviewGroups || []).filter(x => x.pursuit === \"pursued\").map(x => x.id));\n    return P.groups.map((g, i) => ({ id: \"g\" + i, title: g.title, varN: g.varN, pkgs: (g.services || []).flatMap(sv => sv.pkgs || []), firstPkg: ((g.services || [])[0] || {}).pkgs && (g.services[0].pkgs || [])[0] })).filter((_, i) => !pursuedReal.has(P.groups[i].id));\n  }\n  prepSelectedIds(m) {\n    const all = this.prepAllGroups(m).map(g => g.id);\n    const excluded = (this.state.disputeExcludedIds || []).map(rid => this.prepIdFromRealId(rid)).filter(Boolean);\n    return all.filter(gid => !excluded.includes(gid));\n  }\n  prepSelection(m) {\n    const all = this.prepAllGroups(m);\n    const sel = this.prepSelectedIds(m);\n    const selGroups = all.filter(g => sel.includes(g.id));\n    const amountN = selGroups.reduce((s, g) => s + g.varN, 0);\n    const pkgSet = new Set(), invSet = new Set();\n    selGroups.forEach(g => g.pkgs.forEach(p => { pkgSet.add(p.t); invSet.add(p.inv); }));\n    return { all, sel, selGroups, amountN, packages: pkgSet.size, invoices: invSet.size };\n  }\n  prepToggleGroup = (e) => {\n    const id = e.currentTarget.dataset.id;\n    const m = this.prepMemo(); if (!m) return;\n    const realId = this.prepRealGroupIds([id])[0]; if (!realId) return;\n    this.setState(s => {\n      const ex = s.disputeExcludedIds || [];\n      const on = ex.includes(realId);\n      this.trackEvent(on ? \"variance_group_selected\" : \"variance_group_deselected\", { account: \"Implentio\", user: \"tori@implentio.com\", creditMemoId: m.id, version: m.version, groupId: id });\n      const nextEx = on ? ex.filter(x => x !== realId) : [...ex, realId];\n      return { disputeExcludedIds: nextEx, disputeDraftDate: nextEx.length ? (s.disputeDraftDate || this.todayShort()) : null };\n    }, this.prepSyncAttachment);\n  };\n  prepToggleSelectAll = () => {\n    const m = this.prepMemo(); if (!m) return;\n    const all = this.prepAllGroups(m).map(g => g.id);\n    const realIds = this.prepRealGroupIds(all);\n    this.setState(s => {\n      const ex = s.disputeExcludedIds || [];\n      const allOn = realIds.every(rid => !ex.includes(rid));\n      const otherEx = ex.filter(rid => !realIds.includes(rid));\n      return { disputeExcludedIds: allOn ? [...otherEx, ...realIds] : otherEx };\n    }, this.prepSyncAttachment);\n  };\n  prepNext = () => {\n    const m = this.prepMemo(); const step = this.state.prepStep || 1;\n    if (step === 1) {\n      if (!this.prepSelectedIds(m).length) return;\n      this.trackEvent(\"prepare_for_3pl_review_completed\", this.prepEventProps(m));\n      this.trackEvent(\"prepare_for_3pl_selection_completed\", { account: \"Implentio\", user: \"tori@implentio.com\", creditMemoId: m.id, version: m.version, selectedGroupIds: this.prepSelectedIds(m), selectedGroupCount: this.prepSelectedIds(m).length });\n    }\n    this.setState({ prepStep: Math.min(3, step + 1) });\n  };\n  prepBack = () => {\n    if (this.state.findingsUnavailable && (this.state.prepStep || 1) <= 2) { this.closePrep(); return; }\n    this.setState(s => ({ prepStep: Math.max(1, (s.prepStep || 1) - 1) }));\n  };\n  onPrepTo = (e) => { const v = e.target.value; this.setState({ prepTo: v }); };\n  onPrepCc = (e) => { this.setState({ prepCc: e.target.value }); };\n  onPrepSubject = (e) => { const v = e.target.value; this.setState({ prepSubject: v }); };\n  onPrepIntro = (e) => { const v = e.target.value; this.setState({ prepIntro: v }); };\n  prepBodyText(m) { const sel = this.prepSelection(m); const complete = sel.selGroups.length === sel.all.length; return (this.state.prepIntro || \"\") + \"\\n\\n\" + this.prepLockedText(m, sel, { complete }); }\n  copyPreparedEmail = () => {\n    const m = this.prepMemo(); if (!m) return;\n    const cc = this.state.prepCc || \"\";\n    const text = \"To: \" + (this.state.prepTo || \"\") + \"\\nCC: \" + cc + \"\\nSubject: \" + (this.state.prepSubject || \"\") + \"\\nAttachment: \" + this.evidenceFileName(m, this.prepSelection(m)) + \"\\n\\n\" + this.prepBodyText(m);\n    const done = () => { this.trackEvent(\"prepared_email_copied\", this.prepEventProps(m)); this.showToast(\"ok\", \"Email copied. Paste it into your email account and attach the evidence file.\"); if (!this.prepEmailAccount()) this.setState({ prepManualActionTaken: true }); };\n    if (navigator.clipboard && navigator.clipboard.writeText) navigator.clipboard.writeText(text).then(done).catch(() => this.showToast(\"warn\", \"The email could not be copied. Select the text and copy it manually.\"));\n    else this.showToast(\"warn\", \"The email could not be copied. Select the text and copy it manually.\");\n  };\n  downloadPreparedEvidence = () => {\n    const m = this.prepMemo(); if (!m) return;\n    if (this.state.prepZipPhase === \"preparing\" || this.state.prepZipPhase === \"failed\") return;\n    const D = window.CM_DATA; const href = (window.__resources && window.__resources.workbook) || (D && D.asset);\n    const name = this.evidenceFileName(m, this.prepSelection(m));\n    const doState = () => this.setState(s => ({ downloadedMemos: s.downloadedMemos.includes(m.id) ? s.downloadedMemos : [...s.downloadedMemos, m.id], memoDlEvents: { ...s.memoDlEvents, [m.id]: this.dlEvent() }, prepManualActionTaken: this.prepEmailAccount() ? s.prepManualActionTaken : true }), () => { this.trackEvent(\"prepared_evidence_downloaded\", this.prepEventProps(m)); this.showToast(\"ok\", \"Downloading \" + name); });\n    if (m.id === this.featuredId()) this.saveFile(href, name).then(doState).catch(() => this.showToast(\"warn\", \"The evidence package could not be downloaded. Please try again or contact your Implentio customer representative.\"));\n    else doState();\n  };\n  prepVals() {\n    const st = this.state; const m = this.prepMemo();\n    if (!m) return { open: false, steps: [], groups: [], rep: null };\n    const findingsUnavailable = !!st.findingsUnavailable;\n    const step = st.prepStep || 1;\n    const sel = this.prepSelection(m);\n    const emailAcct = this.prepEmailAccount();\n    const complete = sel.selGroups.length === sel.all.length;\n    const zipPhase = complete ? \"ready\" : (st.prepZipPhase || \"preparing\");\n    const attachment = {\n      complete, filename: this.evidenceFileName(m, sel),\n      ctaLabel: \"Download files\",\n      supportLabel: complete ? \"Complete credit memo evidence\" : (sel.selGroups.length + \" selected variance-group Excel files\"),\n      includesText: complete ? \"\" : (\"Includes \" + sel.selGroups.length + \" selected variance-group Excel files\"),\n      groupNames: sel.selGroups.map(g => g.title),\n      summaryLabel: complete ? \"Complete evidence package · 1 file\" : (\"Selected evidence package · \" + sel.selGroups.length + \" file\" + (sel.selGroups.length === 1 ? \"\" : \"s\")),\n      preparing: !complete && zipPhase === \"preparing\", failed: !complete && zipPhase === \"failed\", ready: complete || zipPhase === \"ready\",\n    };\n    attachment.viewFiles = [attachment.filename, ...attachment.groupNames];\n    const selIds = sel.sel;\n    const groups = sel.all.map(g => ({\n      id: g.id, title: g.title, amount: this.fmt(g.varN),\n      meta: g.pkgs.length.toLocaleString(\"en-US\") + \" packages · \" + new Set(g.pkgs.map(p => p.inv)).size + \" invoices\",\n      selected: selIds.includes(g.id), opacity: selIds.includes(g.id) ? \"1\" : \"0.45\",\n    }));\n    const topSelected = sel.selGroups.slice().sort((a, b) => b.varN - a.varN)[0];\n    let rep = null;\n    if (topSelected && topSelected.firstPkg) {\n      const p = topSelected.firstPkg;\n      rep = {\n        groupTitle: topSelected.title, track: p.t, inv: p.inv, so: p.so, billed: this.fmt(p.ti), expected: this.fmt(p.te), variance: this.fmt(p.tv),\n        issue: (p.car || \"Carrier\") + \" \" + this.tcase(p.sv) + \" from \" + p.wh + \". Base freight was billed above the expected rate for this service and weight; the remaining charges matched.\",\n      };\n    }\n    const labels = findingsUnavailable ? [\"Prepare email\", \"Ready to send\"] : [\"Review package\", \"Prepare email\", \"Ready to send\"];\n    const stepOffset = findingsUnavailable ? 1 : 0;\n    const displayStep = step - stepOffset;\n    const noSelection = sel.sel.length === 0;\n    return {\n      open: true, step1: !findingsUnavailable && step === 1, step2: step === 2, step3: step === 3,\n      steps: labels.map((l, i) => ({\n        n: String(i + 1), label: l,\n        cls: (i + 1) === displayStep ? \"on\" : ((i + 1) < displayStep ? \"done\" : \"off\"),\n        numStyle: { display: \"inline-flex\", alignItems: \"center\", justifyContent: \"center\", width: \"22px\", height: \"22px\", borderRadius: \"999px\", flex: \"none\", font: \"700 11px var(--imp-font-body)\", border: \"1.5px solid \" + ((i + 1) <= displayStep ? \"var(--imp-purple-500)\" : \"var(--imp-gray-300)\"), background: (i + 1) === displayStep ? \"var(--imp-purple-500)\" : \"#fff\", color: (i + 1) === displayStep ? \"#fff\" : ((i + 1) < displayStep ? \"var(--imp-purple-500)\" : \"var(--imp-fg-subtle)\") },\n        labelStyle: { font: (i + 1) === displayStep ? \"600 13px var(--imp-font-display)\" : \"500 13px var(--imp-font-body)\", color: (i + 1) <= displayStep ? \"var(--imp-ink)\" : \"var(--imp-fg-subtle)\" },\n      })),\n      memoId: m.id, version: m.version, period: m.period, biller: m.provider,\n      total: this.fmt(m.netN || 0), invoices: (m.invoices || 0), packages: (m.orders || 0).toLocaleString(\"en-US\"),\n      file: attachment.filename, attachment, groups, hasGroups: groups.length > 0, rep, hasRep: !!rep,\n      selectAllLabel: selIds.length === sel.all.length ? \"Deselect all\" : \"Select all\",\n      selCount: sel.selGroups.length, selAmount: this.fmt(sel.amountN), selPackages: sel.packages.toLocaleString(\"en-US\"), selInvoices: sel.invoices,\n      noSelection, continueStyle: noSelection ? { opacity: 0.45, cursor: \"not-allowed\" } : {},\n      showCompleteMemoNotice: findingsUnavailable,\n      to: st.prepTo || \"\", cc: st.prepCc || \"\", supportCc: this.supportCc(),\n      subject: st.prepSubject || \"\", intro: st.prepIntro || \"\", locked: this.prepLockedText(m, sel, attachment),\n      ccSummary: st.prepCc || \"\",\n      hasEmailAccount: !!emailAcct, emailAccount: emailAcct, noEmailAccount: !emailAcct && !st.prepManualChosen, manualChosen: !!st.prepManualChosen && !emailAcct,\n      hasCsm: !!this.prepCsm(), csmName: (this.prepCsm() || {}).name || \"\", csmEmail: (this.prepCsm() || {}).email || \"\", csmOptIn: !!st.prepCsmOptIn,\n      supportRecipients: this.supportRecipients(),\n      csmCheckboxLabel: this.prepCsm() ? \"CC my Implentio support team\" : \"CC Implentio Support\",\n      supportCopyText: this.prepCsm()\n        ? \"Include your Customer Success Manager and Implentio Support for help answering questions about the findings and supporting your credit request.\"\n        : \"Include Implentio Support for help answering questions about the findings and supporting your credit request.\",\n      supportSummaryText: this.prepCsm()\n        ? (this.prepCsm().name + \" and Implentio Support will be copied on this request.\")\n        : \"Implentio Support will be copied on this request.\",\n      manualPursueAsk: !emailAcct && !!st.prepManualActionTaken,\n      notConnected: !emailAcct,\n      manualSentDone: !emailAcct && !!st.prepManualSentDone,\n      manualIdle: !emailAcct && !st.prepManualSentDone,\n      step3Title: emailAcct ? (\"Review and send to \" + m.provider) : \"Your dispute package is ready\",\n      sendIdle: (st.prepSendPhase || \"idle\") === \"idle\", sendProcessing: st.prepSendPhase === \"processing\", sendSent: st.prepSendPhase === \"sent\", sendFailed: st.prepSendPhase === \"failed\",\n      notSendSent: st.prepSendPhase !== \"sent\", notSendIdle: (st.prepSendPhase || \"idle\") !== \"idle\",\n      sendBtnLabel: st.prepSendPhase === \"processing\" ? \"Sending…\" : \"Send dispute\",\n      sentEmail: st.prepSentEmail || \"\", sentAt: st.prepSentAt || \"\", sentCc: st.prepSentCc || \"\",\n    };\n  }\n  retryReport = () => { this.setState({ reportStatus: \"generating\" }); setTimeout(() => this.setState({ reportStatus: \"ready\" }, () => this.showToast(\"ok\", \"Report generated\")), 1600); };\n\n  showToast(kind, msg) { clearTimeout(this._toastTimer); const dot = kind === \"warn\" ? \"var(--imp-warning)\" : \"var(--imp-success)\"; this.setState({ toast: { kind, msg, dotStyle: \"width:9px;height:9px;border-radius:999px;flex:none;background:\" + dot + \";\" } }); this._toastTimer = setTimeout(() => this.setState({ toast: null }), 3600); }\n  componentWillUnmount() { window.removeEventListener(\"resize\", this._onResize); clearTimeout(this._toastTimer); clearTimeout(this._dataTimer); document.removeEventListener(\"click\", this._onDocClick); document.removeEventListener(\"keydown\", this._onKeyDown); }\n  _onResize = () => { const w = window.innerWidth; if (w !== this.state.vw) this.setState({ vw: w }); };\n  componentDidMount() { document.addEventListener(\"click\", this._onDocClick); document.addEventListener(\"keydown\", this._onKeyDown); window.addEventListener(\"resize\", this._onResize); this._onResize(); this._ensureData(0); }\n  _ensureData(n) {\n    if (this.state.reviewGroups && this.state.reviewGroups.length) return;\n    if (window.CM_DATA && window.CM_PKG) { this.setState({ invoices: this.buildInvoices(), reviewGroups: this.buildReview(), underGroups: this.buildUnder() }); return; }\n    if (n < 60) this._dataTimer = setTimeout(() => this._ensureData(n + 1), 100);\n  }\n\n  activeMemo() {\n    const base = this.memoList().find(m => m.id === this.state.activeMemoId) || this.memoList().find(m => m.id === this.featuredId());\n    return base;\n  }\n\n  renderVals() {\n    const st = this.state;\n    const okS = \"background:var(--imp-success-bg); color:var(--imp-success); border-color:var(--imp-success);\";\n    const procS = \"background:var(--imp-warning-bg); color:#8a5a05; border-color:var(--imp-warning);\";\n    const dlS = \"background:var(--imp-purple-100); color:var(--imp-purple-500); border-color:var(--imp-purple-300);\";\n    const genS = \"background:var(--imp-warning-bg); color:#8a5a05; border-color:var(--imp-warning);\";\n    const reportPill = { ready: [\"Ready\", okS], generating: [\"Audit In-Progress\", genS], downloaded: [\"Downloaded\", dlS], failed: [\"Failed\", \"background:var(--imp-error-bg); color:var(--imp-error); border-color:var(--imp-error);\"] };\n\n    // ----- tracker -----\n    let memos = this.memoList();\n    if (st.tStatus !== \"all\") memos = memos.filter(m => m.status === st.tStatus);\n    if (st.tReport !== \"all\") memos = memos.filter(m => m.report === st.tReport);\n    if (st.tCarrier !== \"all\") memos = memos.filter(m => m.carriers.includes(st.tCarrier));\n    if (st.tProvider !== \"all\") memos = memos.filter(m => m.provider === st.tProvider);\n    if (st.tPeriod !== \"all\") memos = memos.filter(m => m.period === st.tPeriod);\n    if (st.tRange !== \"all\") { const cut = { \"30\": 2, \"60\": 3, \"90\": 4, \"365\": 5 }[st.tRange] || memos.length; memos = memos.slice(0, cut); }\n    const dash = \"—\";\n    const colorMap = { purple: \"var(--imp-purple-500)\", amber: \"#B45309\", green: \"var(--imp-success)\" };\n    const memoView = memos.map(m => {\n      const pct = (m.netN != null && m.invoicedN) ? (m.netN / m.invoicedN * 100).toFixed(1) + \"%\" : null;\n      const prepared = m.report === \"generating\" ? null : \"Report prepared \" + m.completedText.replace(/^Completed /, \"\").replace(/^Prepared /, \"\");\n      const tns = m.golden ? this.trackerDisputeCta() : null;\n      const cta = tns || { key: \"ready\", statusLabel: \"READY TO DISPUTE\", color: \"purple\", supporting: \"No dispute has been started.\", primaryKind: \"prep\", primaryLabel: \"Prepare dispute for Biller\" };\n      const ctaHandler = { prep: this.openPrep, draft: this.openPrep, outcome: (this.state.findingsUnavailable ? this.openMemoLevelOutcome : this.openMemoOutcome), view: (this.state.findingsUnavailable ? this.openMemoLevelDetails : this.openDisputeDetails) }[cta.primaryKind];\n      const pl = (n, w) => n + \" \" + w + (n === 1 ? \"\" : \"s\");\n      const allNoVariance = !!m.allNoVariance;\n      const noVar = m.invoicesNoVariance != null ? m.invoicesNoVariance : 0;\n      const audited = m.invoicesNoVariance != null ? (m.invoices || 0) + noVar : null;\n      const showCoverage = !allNoVariance && audited != null && noVar > 0;\n      return {\n        id: m.id, anchor: \"memo-card-\" + m.id, provider: m.provider, periodBold: m.period, slaCadence: m.cadence, carriers: m.carriers,\n        generating: m.report === \"generating\", ready: m.report === \"ready\", downloaded: m.report === \"downloaded\", updated: m.report === \"updated\",\n        downloadedBy: this.dlAttribution(m.id, m.dlEvent),\n        statusReady: m.report === \"ready\" || m.report === \"updated\",\n        allNoVariance, hasVariance: !allNoVariance,\n        showFinancials: (m.report === \"ready\" || m.report === \"downloaded\" || m.report === \"updated\") && !allNoVariance,\n        allClearLine1: allNoVariance ? pl(audited, \"invoice\") + \" audited\" : null,\n        allClearLine2: allNoVariance ? \"All \" + pl(audited, \"invoice\") + \" had no significant variance\" : null,\n        auditedOnlyText: allNoVariance ? pl(audited, \"invoice\") + \" audited\" : null,\n        allClearBody: allNoVariance ? \"All \" + pl(audited, \"invoice\") + \" in this audit were reviewed and were within the significant-variance threshold.\" : null,\n        showCoverage, coverageText: showCoverage ? (pl(audited, \"invoice\") + \" audited · \" + noVar + \" had no significant variance\") : null,\n        actionsEnabled: m.report === \"ready\" || m.report === \"downloaded\" || m.report === \"updated\",\n        ctaColor: colorMap[cta.color] || colorMap.purple, ctaStatusLabel: cta.statusLabel, ctaSupporting: cta.supporting,\n        ctaPrimaryLabel: cta.primaryLabel, ctaHandler,\n        ctaIsPrep: cta.primaryKind === \"prep\", ctaIsDraft: cta.primaryKind === \"draft\", ctaIsOutcome: cta.primaryKind === \"outcome\", ctaIsView: cta.primaryKind === \"view\",\n        hasCtaContextual: !!cta.contextual, ctaContextual: cta.contextual ? cta.contextual.text : null, ctaContextualLink: cta.contextual ? cta.contextual.link : null, ctaContextualHandler: this.openMemoOutcome,\n        isNew: !!m.isNew, isUpdated: m.report === \"updated\", showDemo: !!(m.golden || m.placeholder), demoLabel: m.golden ? \"Source-backed demo\" : \"Demo placeholder\",\n        versionLabel: (m.versionNum && m.versionNum > 1) ? (m.report === \"downloaded\" ? (m.version + \" · Downloaded\") : m.completedText) : null,\n        preparedText: (m.versionNum && m.versionNum > 1) ? null : prepared,\n        over: m.netN == null ? dash : this.fmt(m.netN),\n        overSub: (m.orders == null ? dash : m.orders.toLocaleString(\"en-US\")) + \" affected packages on \" + (m.invoices == null ? dash : m.invoices) + \" invoices\",\n        billedSub: (m.invoicedN == null ? dash : this.fmt(m.invoicedN)) + \" billed · \" + (m.expectedN != null ? this.fmt(m.expectedN) : (m.invoicedN == null ? dash : this.fmt(m.invoicedN - (m.netN || 0)))) + \" expected\",\n        analyzedText: (m.invoices == null ? dash : m.invoices) + \" invoices · \" + (m.orders == null ? dash : m.orders) + \" packages analyzed\",\n        accentStyle: m.report === \"generating\" ? \"var(--imp-warning)\" : ((m.isNew || m.report === \"updated\") ? \"var(--imp-orange-500)\" : (m.report === \"downloaded\" ? \"transparent\" : \"var(--imp-gray-300)\")),\n      };\n    });\n\n    // ----- executive summary (all memos) -----\n    const allMemos = this.memoList();\n    const execT = allMemos.reduce((a, m) => { a.net += m.netN || 0; a.invoiced += m.invoicedN || 0; a.over += m.overN || 0; a.under += m.underN || 0; return a; }, { net: 0, invoiced: 0, over: 0, under: 0 });\n    const memosReady = allMemos.filter(m => m.status === \"complete\").length;\n    const reportsDownloaded = allMemos.filter(m => m.report === \"downloaded\").length;\n    const exec = { count: allMemos.length, net: this.fmt(execT.net), invoiced: this.fmt(execT.invoiced), over: this.fmt(execT.net), under: this.fmt(execT.under), memosReady, reportsDownloaded, creditsRealized: null };\n\n    // ----- active memo -----\n    const am0 = this.activeMemo();\n    const activeMemo = am0 ? {\n      id: am0.id, period: am0.period, cadence: am0.cadence, version: am0.version, completedText: am0.completedText,\n      statusLabel: am0.statusLabel, statusStyle: am0.statusStyle,\n      invoiced: am0.invoicedN == null ? dash : this.fmt(am0.invoicedN),\n      expected: am0.expectedN != null ? this.fmt(am0.expectedN) : (am0.invoicedN == null ? dash : this.fmt(am0.invoicedN - am0.overN + am0.underN)),\n      over: am0.netN == null ? dash : this.fmt(am0.netN),\n      under: am0.underN == null ? dash : this.fmt(am0.underN),\n      net: am0.netN == null ? dash : this.fmt(am0.netN),\n      invoices: am0.invoices == null ? dash : am0.invoices, orders: am0.orders == null ? dash : am0.orders,\n      billed: am0.invoicedN == null ? dash : this.fmt(am0.invoicedN),\n      packagesAffected: am0.orders == null ? dash : am0.orders.toLocaleString(\"en-US\"),\n      golden: !!am0.golden, isUpdated: am0.report === \"updated\",\n      changeSummary: am0.changeSummary || null, updatedText: am0.updatedText || null, versionNum: am0.versionNum || 1,\n      allNoVariance: !!am0.allNoVariance, hasVariance: !am0.allNoVariance,\n      auditedOnlyText: am0.allNoVariance ? ((am0.invoices || 0) + (am0.invoicesNoVariance || 0)) + \" invoice\" + (((am0.invoices || 0) + (am0.invoicesNoVariance || 0)) === 1 ? \"\" : \"s\") + \" audited\" : null,\n      allClearBody: am0.allNoVariance ? \"All \" + ((am0.invoices || 0) + (am0.invoicesNoVariance || 0)) + \" invoice\" + (((am0.invoices || 0) + (am0.invoicesNoVariance || 0)) === 1 ? \"\" : \"s\") + \" in this audit were reviewed and were within the significant-variance threshold.\" : null,\n      showCoverage: !am0.allNoVariance && am0.invoicesNoVariance != null && am0.invoicesNoVariance > 0,\n      coverageText: (!am0.allNoVariance && am0.invoicesNoVariance != null && am0.invoicesNoVariance > 0)\n        ? (((am0.invoices || 0) + am0.invoicesNoVariance) + \" invoice\" + (((am0.invoices || 0) + am0.invoicesNoVariance) === 1 ? \"\" : \"s\") + \" audited \\u00b7 \" + am0.invoicesNoVariance + \" had no significant variance\")\n        : null,\n    } : null;\n    if (activeMemo) {\n      const rv = am0.reportVersions || [{ num: am0.versionNum || 1, label: am0.version || \"Version 1\", current: true, dateText: am0.completedText, changeSummary: am0.changeSummary || null }];\n      const latest = rv.find(v => v.current) || rv[0];\n      activeMemo.latestVersion = { num: latest.num, label: latest.label, dateText: latest.dateText, changeSummary: latest.changeSummary || null };\n      activeMemo.previousVersions = rv.filter(v => v !== latest).map(v => ({ num: v.num, label: v.label, dateText: v.dateText, note: v.note || \"\" }));\n      activeMemo.hasPrevious = activeMemo.previousVersions.length > 0;\n      const D0 = window.CM_DATA;\n      const trk = new Set();\n      if (D0 && am0.golden) (D0.invoices || []).forEach(v => (v.orders || []).forEach(o => { if (o.track && o.varN > 0 && /overcharge/i.test(o.outcome || \"\")) trk.add(o.track); }));\n      activeMemo.packages = trk.size ? trk.size.toLocaleString(\"en-US\") : (am0.orders == null ? dash : am0.orders.toLocaleString(\"en-US\"));\n    }\n\n    // ----- invoices -----\n    const resultStyleMap = {\n      ok: okS + \" text-transform:none; letter-spacing:0; font-weight:600;\",\n      below: procS + \" text-transform:none; letter-spacing:0; font-weight:600;\",\n      overcharge: \"background:#FFE9D6; color:var(--imp-orange-500); border-color:var(--imp-orange-300); text-transform:none; letter-spacing:0; font-weight:600;\",\n      doc: \"background:var(--imp-gray-100); color:var(--imp-fg-muted); border-color:var(--imp-gray-300); text-transform:none; letter-spacing:0; font-weight:600;\",\n      undercharge: \"background:var(--imp-purple-100); color:var(--imp-purple-500); border-color:var(--imp-purple-300); text-transform:none; letter-spacing:0; font-weight:600;\",\n    };\n    const filtered = this.filterInvoices(st);\n    const fmtCell = (n) => n == null ? dash : this.fmt(n);\n    const invoiceView = filtered.map(r => ({\n      id: r.id, inv: r.inv, date: r.date, carrier: r.carrier, biller: r.biller, warehouse: r.warehouse,\n      invoiced: this.fmt(r.invoicedN), expected: fmtCell(r.expectedN), variance: r.varN == null ? dash : (r.varN === 0 ? dash : this.fmt(r.varN)),\n      resultLabel: r.resultLabel, resultStyle: resultStyleMap[r.kind],\n      selected: r.selected,\n    }));\n    const selCount = st.invoices.filter(r => r.selected).length;\n    const allChecked = filtered.length > 0 && filtered.every(r => r.selected);\n    const stats = filtered.reduce((a, r) => {\n      a.count++; a.invoiced += r.invoicedN;\n      if (r.expectedN != null) a.expected += r.expectedN;\n      if (r.kind === \"overcharge\" && r.varN > 0) a.over += r.varN;\n      a.over2 += r.overN || 0; a.under += r.underN || 0;\n      return a;\n    }, { count: 0, invoiced: 0, expected: 0, over: 0, over2: 0, under: 0 });\n    const invStats = { count: stats.count, invoiced: this.fmt(stats.invoiced), expected: this.fmt(stats.expected), over: this.fmt(stats.over2), under: this.fmt(stats.under) };\n\n    // ----- findings -----\n    const recRow = (r) => ({\n      id: r.id, inv: r.inv, date: r.date, carrier: r.carrier, biller: r.biller, warehouse: r.warehouse,\n      invoiced: this.fmt(r.invoicedN), expected: r.expectedN == null ? dash : this.fmt(r.expectedN),\n      variance: r.varN == null ? dash : (r.varN === 0 ? dash : this.fmt(r.varN)),\n      resultLabel: r.resultLabel, resultStyle: resultStyleMap[r.kind],\n    });\n    const grpRow = (r) => ({\n      id: r.id, inv: r.inv, date: r.date, carrier: r.carrier, biller: r.biller, warehouse: r.warehouse,\n      invoiced: this.fmt(r.invoicedN), expected: this.fmt(r.expectedN),\n      variance: r.varN == null || r.varN === 0 ? dash : this.fmt(r.kind === \"undercharge\" ? -Math.abs(r.varN) : Math.abs(r.varN)),\n      resultLabel: r.resultLabel, resultStyle: resultStyleMap[r.kind],\n    });\n    const titleCase = (v) => String(v).toLowerCase().split(\" \").map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(\" \");\n    const optList = (vals, label) => Array.from(new Set(vals)).sort().map(v => ({ value: v, label: label ? label(v) : v }));\n    const carrierOpts = optList([].concat.apply([], st.reviewGroups.map(g => g.carriers || [])));\n    const serviceOpts = optList([].concat.apply([], st.reviewGroups.map(g => g.serviceList || [])), titleCase);\n    const CHG = this.chargeDefs();\n    const chargeOpts = CHG.map(c => ({ value: c[0], label: c[2] }));\n    const hl = st.fCategory;\n    const hlLabel = hl === \"all\" ? null : (CHG.find(c => c[0] === hl) || [])[2];\n    const hlOn = \"background:var(--imp-warning-bg); box-shadow:inset 0 0 0 1px var(--imp-orange-300);\";\n    const segBase = \"display:inline-flex; align-items:center; justify-content:center; gap:8px; flex:1 1 190px; min-width:170px; padding:9px 14px; border-radius:9px; cursor:pointer; font:600 13px var(--imp-font-body); text-align:center; transition:background 120ms ease, border-color 120ms ease, color 120ms ease;\";\n    const segStyle = (on) => segBase + (on\n      ? \"background:var(--imp-purple-100); border:1.5px solid var(--imp-purple-500); color:var(--imp-purple-500);\"\n      : \"background:#fff; border:1.5px solid var(--imp-gray-300); color:var(--imp-fg-muted);\");\n    const chevStyle = (on) => \"flex:none; transition:transform 160ms ease;\" + (on ? \"transform:rotate(180deg);\" : \"\");\n    const rollupProvider = (this.activeMemo() || {}).provider || \"Biller\";\n    const groupsFiltered = st.reviewGroups.filter(g => {\n      if (!((st.fCarrier === \"all\" || (g.carriers || []).indexOf(st.fCarrier) >= 0) &&\n        (st.fService === \"all\" || (g.serviceList || []).indexOf(st.fService) >= 0))) return false;\n      if (st.fDisputeStatus !== \"all\") {\n        const inDisputeSel = !(st.disputeExcludedIds || []).includes(g.id);\n        const sl = this.groupStatusLine({ ...g, threePl: g.threePl || rollupProvider, inDisputeSel });\n        if (sl.key !== st.fDisputeStatus) return false;\n      }\n      return true;\n    });\n    const reviewGroups = groupsFiltered.map(g => {\n      const q = (g.search || \"\").trim().toLowerCase();\n      const pkgMatch = (o) => !q || String(o.so || \"\").toLowerCase().includes(q) || String(o.t || \"\").toLowerCase().includes(q) || String(o.inv || \"\").toLowerCase().includes(q);\n      const allRecs = (g.records || []).filter(rec => !q || String(rec.serviceLabel || \"\").toLowerCase().includes(q) || (rec.orders || []).some(pkgMatch));\n      const shownRecs = allRecs;\n      const pkgLimit = g.pkgLimit || 10;\n      const mixEntry = hl === \"all\" ? null : (g.mixUnfav || []).concat(g.mixFav || []).find(x => x.key === hl);\n      return {\n        ...g,\n        toggleLabel: g.expanded ? \"Hide affected packages\" : \"View affected packages\",\n        explOpen: g.detail === \"expl\", evidenceOpen: g.detail === \"evidence\", detailOpen: g.detail !== \"none\",\n        panelId: g.id + \"-detail-panel\", tabExplId: g.id + \"-tab-expl\", tabEvId: g.id + \"-tab-evidence\",\n        panelLabelledBy: g.detail === \"evidence\" ? (g.id + \"-tab-evidence\") : (g.id + \"-tab-expl\"),\n        evidenceCount: (g.evidence || []).length,\n        explLabel: g.detail === \"expl\" ? \"Hide how we identified this finding\" : \"How we identified this finding\",\n        explChevStyle: \"transform:\" + (g.detail === \"expl\" ? \"rotate(180deg)\" : \"none\") + \"; transition:transform 0.15s ease;\",\n\n        errorGroupCopy: \"Includes the finding and its explanation, the supporting documents used, affected invoices, the complete assigned package population with order and tracking numbers, complete package invoiced, expected and net variance, the charge-level breakdown behind each package, and the available raw calculation data.\",\n        evidence: (g.evidence || []).map(ev => ({ ...ev, unavailable: !ev.available })),\n        billedAmt: this.fmt(g.invoicedN), expectedAmt: this.fmt(g.expectedN),\n        ...(() => { const inDisputeSel = !(st.disputeExcludedIds || []).includes(g.id);\n          const sl = this.groupStatusLine({ ...g, threePl: g.threePl || rollupProvider, inDisputeSel });\n          return { statusPrimary: sl.t.label, statusFg: sl.t.fg, statusBg: sl.t.bg,\n            statusBoxStyle: \"border-color:\" + sl.t.fg + \"; background:\" + sl.t.bg + \";\",\n            statusIconImg: sl.t.icon ? React.createElement('img', { src: \"assets/status-icons/\" + sl.t.icon, alt: \"\", width: 24, height: 24, style: { flex: 'none', width: 24, height: 24, objectFit: 'contain' } }) : null,\n            statusSecondary: sl.secondary,\n            statusActionLabel: sl.actionLabel || \"\", statusShowAction: !!sl.showAction, statusSubtleAction: !!sl.subtleAction,\n            canUpdateOutcome: g.pursuit === \"pursued\", justUpdated: (st.prepUpdatedIds || []).includes(g.id) }; })(),\n        hasHl: !!mixEntry, hlText: mixEntry ? (hlLabel + \" contributes \" + this.posMoney(mixEntry.amount) + (mixEntry.amount < 0 ? \" favourable\" : \" unfavourable\") + \" inside this finding \\u2014 the group net variance of \" + this.posMoney(g.varN) + \" is unchanged.\") : \"\",\n        inDispute: g.pursuit === \"pursued\" ? true : !(st.disputeExcludedIds || []).includes(g.id),\n        disputeBoxBg: g.pursuit !== \"pursued\" && (st.disputeExcludedIds || []).includes(g.id) ? \"#fff\" : \"var(--imp-purple-500)\",\n        disputeLocked: g.pursuit === \"pursued\" || this.groupExpired(g),\n        notExpanded: !g.expanded,\n        disputeControlLabel: g.pursuit === \"pursued\" ? \"Included in dispute package\" : ((st.disputeExcludedIds || []).includes(g.id) ? \"Add variance group to dispute package\" : \"Included in dispute package\"),\n        recordsCountLabel: \"Showing \" + shownRecs.length + \" of \" + allRecs.length + \" service levels\",\n        allRecsShown: allRecs.length > 0,\n        recsTotalLabel: \"All service levels (\" + allRecs.length + \")\",\n        recsTotalPkgs: allRecs.reduce((a, r) => a + (r.pkgCount || 0), 0).toLocaleString(\"en-US\"),\n        recsTotalInv: this.fmt(this.r2(allRecs.reduce((a, r) => a + (r.invoicedN || 0), 0))),\n        recsTotalExp: this.fmt(this.r2(allRecs.reduce((a, r) => a + (r.expectedN || 0), 0))),\n        recsTotalVar: this.posMoney(this.r2(allRecs.reduce((a, r) => a + (r.varN || 0), 0))),\n\n        recordsEmpty: allRecs.length === 0,\n        chgCols: CHG.map(c => ({ key: c[0], label: c[2], hlStyle: hl === c[0] ? hlOn : \"\" })),\n        records: shownRecs.map(rec => {\n          const svcMatch = !q || String(rec.serviceLabel || \"\").toLowerCase().includes(q);\n          const indexed = (rec.orders || []).map((o, i) => ({ o: o, i: i }));\n          const matched = svcMatch ? indexed : indexed.filter(x => pkgMatch(x.o));\n          const shownPkgs = matched.slice(0, pkgLimit);\n          return {\n            id: rec.id, gid: g.id, serviceLabel: rec.serviceLabel,\n            invoiced: this.fmt(rec.invoicedN), expected: this.fmt(rec.expectedN), variance: this.posMoney(rec.varN),\n            open: g.openRecord === rec.id,\n            detailLabel: g.openRecord === rec.id ? \"Hide packages\" : \"Show packages\",\n            pkgCount: rec.pkgCount,\n            orderCountShown: shownPkgs.length,\n            pkgCountLabel: \"Showing \" + shownPkgs.length + \" of \" + matched.length + \" packages\",\n            hasMorePkgs: matched.length > shownPkgs.length,\n            allPkgsShown: matched.length > 0 && matched.length <= shownPkgs.length,\n            orders: shownPkgs.map(x => {\n              const o = x.o;\n              const pkey = (o.t || \"\") + \"|\" + (o.inv || \"\");\n              const chg = CHG.map(c => {\n                const iv = (o[c[0]] || [0, 0])[0], ex = (o[c[0]] || [0, 0])[1];\n                const df = this.r2(iv - ex);\n                const on = hl === c[0];\n                return {\n                  key: c[0], label: c[2],\n                  exp: ex ? this.fmt(ex) : dash, inv: iv ? this.fmt(iv) : dash,\n                  diff: Math.abs(df) < 0.005 ? dash : (df > 0 ? this.posMoney(df) : \"\\u2212\" + this.posMoney(df)),\n                  hlStyle: on ? hlOn : \"\",\n                  diffStyle: (on ? hlOn : \"\") + (Math.abs(df) < 0.005 ? \"\" : (df > 0 ? \"color:var(--imp-orange-500); font-weight:700;\" : \"color:var(--imp-success); font-weight:700;\")),\n                };\n              });\n              const unf = chg.filter(c => c.diff !== dash && c.diff.charAt(0) !== \"\\u2212\").map(c => c.label.toLowerCase() + \" \" + c.diff);\n              const fav = chg.filter(c => c.diff.charAt(0) === \"\\u2212\").map(c => c.label.toLowerCase() + \" \" + c.diff.slice(1));\n              const hlHit = hl !== \"all\" && Math.abs(this.r2(((o[hl] || [0, 0])[0]) - ((o[hl] || [0, 0])[1]))) > 0.005;\n              return {\n                oi: x.i, gid: g.id, rid: rec.id, pkey: pkey,\n                so: o.so, tracking: o.t, inv: o.inv, carrier: o.car, service: o.sv, serviceLabel: titleCase(o.sv),\n                labelDate: this.xdate(o.ld), zone: (o.az || o.ez || dash),\n                weight: (o.wt != null ? o.wt + \" oz\" : dash),\n                invoiced: this.fmt(o.ti), expected: this.fmt(o.te), variance: this.posMoney(o.tv),\n                chg: chg,\n                rowStyle: hlHit ? \"background:var(--imp-warning-bg);\" : \"\",\n                stickyBg: hlHit ? \"background:var(--imp-warning-bg);\" : \"background:#fff;\",\n                reconText: \"Charge differences reconcile to the package result: \" + (unf.length ? unf.join(\", \") + \" unfavourable\" : \"no unfavourable charge difference\") + (fav.length ? \", offset by \" + fav.join(\", \") + \" favourable\" : \"\") + \", netting to \" + this.posMoney(o.tv) + \" for this package.\",\n              };\n            }),\n            reconInv: this.fmt(this.r2(shownPkgs.reduce((acc, x) => acc + (x.o.ti || 0), 0))),\n            reconExp: this.fmt(this.r2(shownPkgs.reduce((acc, x) => acc + (x.o.te || 0), 0))),\n            reconVar: this.posMoney(this.r2(shownPkgs.reduce((acc, x) => acc + (x.o.tv || 0), 0))),\n            reconChg: CHG.map(c => {\n              const iv = this.r2(shownPkgs.reduce((acc, x) => acc + ((x.o[c[0]] || [0, 0])[0] || 0), 0));\n              const ex = this.r2(shownPkgs.reduce((acc, x) => acc + ((x.o[c[0]] || [0, 0])[1] || 0), 0));\n              const df = this.r2(iv - ex);\n              return {\n                exp: ex ? this.fmt(ex) : dash, inv: iv ? this.fmt(iv) : dash,\n                diff: Math.abs(df) < 0.005 ? dash : (df > 0 ? this.posMoney(df) : \"\\u2212\" + this.posMoney(df)),\n                diffStyle: Math.abs(df) < 0.005 ? \"\" : (df > 0 ? \"color:var(--imp-orange-500);\" : \"color:var(--imp-success);\"),\n              };\n            }),\n          };\n        }),\n      };\n    });\n    const underGroups = st.underGroups.map(g => ({ ...g, toggleLabel: g.expanded ? \"Hide affected invoices\" : \"View affected invoices\", records: (g.records || []).map(grpRow) }));\n\n    // ----- credit outcomes dashboard -----\n    const ocm = this.outcomesSummary();\n    let ocRows = ocm.rows;\n    const rowPursuitKey = (g) => g.pursuit === \"pursued\" ? \"pursued\" : \"eligible\";\n    const ocMemoOpts = Array.from(new Set(ocRows.map(g => g.memoId))).sort().map(v => ({ value: v, label: v }));\n    const ocProvOpts = Array.from(new Set(ocRows.map(g => g.threePl))).filter(Boolean).sort().map(v => ({ value: v, label: v }));\n    const ocGroupOpts = Array.from(new Set(ocRows.map(g => g.category || g.title))).sort().map(v => ({ value: v, label: v }));\n    const ocPursuitOpts = [{ value: \"eligible\", label: \"Eligible to pursue\" }, { value: \"pursued\", label: \"Pursued with Biller\" }];\n    const ocOutcomeOpts = [{ value: \"awaiting\", label: \"Awaiting outcome\" }, { value: \"partial\", label: \"Partially collected\" }, { value: \"full\", label: \"Fully collected\" }, { value: \"not_issued\", label: \"Denied by Biller\" }];\n    if (st.ocPeriod !== \"all\") ocRows = ocRows.filter(g => g.memoId === st.ocPeriod);\n    if (st.ocProvider !== \"all\") ocRows = ocRows.filter(g => g.threePl === st.ocProvider);\n    if (st.ocGroup !== \"all\") ocRows = ocRows.filter(g => (g.category || g.title) === st.ocGroup);\n    const donutRows = ocRows;\n    if (st.ocDisposition !== \"all\") {\n      ocRows = ocRows.filter(g => this.dispositionMatch(g, st.ocDisposition));\n    } else {\n      if (st.ocPursuit !== \"all\") ocRows = ocRows.filter(g => rowPursuitKey(g) === st.ocPursuit);\n      if (st.ocOutcome !== \"all\") ocRows = ocRows.filter(g => g.collection && g.collection.status === st.ocOutcome);\n    }\n    const ocTable = ocRows.map(g => {\n      const di = this.disputeInfo(g); const ci = this.collectionInfo(g);\n      const reason = this.groupReason(g);\n      const isNotIssued = !!(g.collection && g.collection.status === \"not_issued\");\n      const canExpand = !!reason;\n      return {\n        id: g.id, title: g.title, memoId: g.memoId, threePl: g.threePl || \"\\u2014\",\n        identified: this.fmt(g.amountN || 0), pursued: g.pursuit === \"pursued\" ? this.fmt(g.amountN || 0) : \"\\u2014\",\n        collected: (g.collection && g.collection.amountN != null) ? this.fmt(g.collection.amountN) : \"\\u2014\",\n        deadline: g.disputeDeadline ? this.fmtDateLong(g.disputeDeadline) : \"\\u2014\",\n        dispute: g.pursuit === \"pursued\" ? ((g.threePl || \"Biller\") + \" dispute \\u00b7 \" + (g.pursuedAt || \"\\u2014\")) : \"\\u2014\",\n        outcomeLabel: ci ? ci.label : \"Eligible to pursue\",\n        outcomeStyle: ci ? ci.style : \"background:var(--imp-purple-100); color:var(--imp-purple-500); border-color:var(--imp-purple-300);\",\n        collectedDate: (g.collection && g.collection.date) ? this.fmtDateLong(g.collection.date) : \"\\u2014\",\n        reason, hasReason: !!reason, noReasonProvided: !reason && isNotIssued, reasonNA: !isNotIssued,\n        actionLabel: g.pursuit === \"pursued\" ? \"View dispute\" : \"Review finding\",\n        canExpand, cannotExpand: !canExpand,\n        notesOpen: canExpand && st.ocOpenNotesId === g.id,\n        chevronStyle: (st.ocOpenNotesId === g.id) ? \"color:var(--imp-purple-500); transform:rotate(90deg); transition:transform 120ms;\" : \"color:var(--imp-purple-500); transition:transform 120ms;\",\n      };\n    });\n    const oc3pl = this.outcomesBy3pl();\n    const ocHasFilters = st.ocPeriod !== \"all\" || st.ocProvider !== \"all\" || st.ocGroup !== \"all\" || st.ocPursuit !== \"all\" || st.ocOutcome !== \"all\" || st.ocDisposition !== \"all\";\n    const ocActiveCount = [st.ocPeriod, st.ocProvider, st.ocGroup, st.ocPursuit, st.ocOutcome].filter(v => v !== \"all\").length + (st.ocDisposition !== \"all\" ? 1 : 0);\n    const ocSummaryLabel = ocActiveCount + \" active filter\" + (ocActiveCount === 1 ? \"\" : \"s\") + \" \\u00b7 Showing \" + ocTable.length + \" of \" + ocm.rows.length + \" findings\";\n    const activeFieldStyle = \"padding:8px 10px; width:100%; border-color:var(--imp-purple-400); box-shadow:0 0 0 1px var(--imp-purple-200);\";\n    const inactiveFieldStyle = \"padding:8px 10px; width:100%;\";\n    const activeCompactStyle = \"padding:8px 10px; width:auto; border-color:var(--imp-purple-400); box-shadow:0 0 0 1px var(--imp-purple-200);\";\n    const inactiveCompactStyle = \"padding:8px 10px; width:auto;\";\n    const donut = this.dispositionDonut(donutRows);\n    const disp = this.dispositionAmounts(donutRows);\n    const identifiedN = this.r2(disp.eligible + disp.awaiting + disp.collected + disp.denied);\n    const activeKey = st.ocHoverSlice || st.ocSelectedSlice;\n    const activeSlice = activeKey ? donut.slices.find(s => s.key === activeKey) : null;\n    donut.slices = donut.slices.map(s => ({ ...s, active: s.key === activeKey, isSelected: s.key === st.ocSelectedSlice, strokeWidth: s.key === activeKey ? 18 : 14, legendBg: s.key === activeKey ? \"var(--imp-gray-100)\" : \"transparent\" }));\n    donut.slicesZ = donut.slices.slice().sort((a, b) => b.amountN - a.amountN);\n    donut.slicesZVisible = donut.slicesZ.filter(s => s.amountN > 0);\n\n    // ----- update dispute outcome modal -----\n    const omStatusMeta = {\n      awaiting: { label: \"Awaiting outcome\", pillBg: \"#F2F0FF\", pillFg: \"#5B4AE6\", accent: \"#5B4AE6\" },\n      full: { label: \"Fully collected\", pillBg: \"#ECFDF3\", pillFg: \"#087443\", accent: \"#087443\" },\n      partial: { label: \"Partly collected\", pillBg: \"#FFF7E6\", pillFg: \"#B45309\", accent: \"#B45309\" },\n      not_issued: { label: \"Biller declined\", pillBg: \"#FFF1F0\", pillFg: \"#B42318\", accent: \"#B42318\" },\n    };\n    const omPillMap = {\n      \"Awaiting outcome\": omStatusMeta.awaiting, \"Fully collected\": omStatusMeta.full,\n      \"Biller declined\": omStatusMeta.not_issued, \"Partially resolved\": omStatusMeta.partial,\n    };\n    const omOptionDefs = [\n      { value: \"awaiting\", label: \"Awaiting outcome\" },\n      { value: \"full\", label: \"Fully collected\" },\n      { value: \"partial\", label: \"Partly collected\" },\n      { value: \"not_issued\", label: \"Biller declined\" },\n    ];\n    const omOptionMeta = omOptionDefs.map(o => ({ ...o, ...omStatusMeta[o.value], isFull: o.value === \"full\", isPartial: o.value === \"partial\", isNotIssued: o.value === \"not_issued\", isAwaiting: o.value === \"awaiting\" }));\n    const omBtnStyle = (accent, sel) => \"display:flex; align-items:center; gap:6px; padding:9px 10px; border-radius:9px; cursor:pointer; font:600 12.5px var(--imp-font-body); white-space:nowrap; background:#fff; \" + (sel ? (\"border:1.5px solid \" + accent + \"; color:\" + accent + \";\") : \"border:1.5px solid var(--imp-gray-300); color:var(--imp-fg-muted);\");\n    const omRowFor = (om, g) => {\n      const d = om.drafts[g.id] || {};\n      const isFull = d.status === \"full\", isPartial = d.status === \"partial\", isNotIssued = d.status === \"not_issued\", isAwaiting = d.status === \"awaiting\" || !d.status;\n      const m = omStatusMeta[d.status] || omStatusMeta.awaiting;\n      const pursuedAmt = g.amountN || 0;\n      const parsedAmt = parseFloat(d.amount || \"\");\n      const hasAmt = d.amount !== undefined && d.amount !== null && d.amount !== \"\" && !isNaN(parsedAmt);\n      let amtError = \"\";\n      if (isPartial) {\n        if (!hasAmt || parsedAmt <= 0) amtError = \"Enter a credit amount greater than $0.00.\";\n        else if (parsedAmt > pursuedAmt + 0.005) amtError = \"Credit received cannot exceed the amount pursued of \" + this.fmt(pursuedAmt) + \".\";\n      }\n      const remaining = (isPartial && hasAmt && !amtError) ? Math.round((pursuedAmt * 100 - parsedAmt * 100)) / 100 : 0;\n      return {\n        id: g.id, title: g.title || \"Variance group\", amountPursuedDisplay: this.fmt(pursuedAmt),\n        statusLabel: m.label, statusIconColor: m.accent, statusPillBg: m.pillBg, creditLabel: isPartial ? \"Credit received *\" : \"Credit received\",\n        ddOpen: om.openDropdownId === g.id,\n        ddStyle: (om.openDropdownId === g.id && om.dropdownPos) ? (\"top:\" + om.dropdownPos.top + \"px; left:\" + om.dropdownPos.left + \"px; width:\" + om.dropdownPos.width + \"px; max-height:190px;\") : \"\",\n        isFull, isPartial, isNotIssued, isAwaiting,\n        outcomeOptions: omOptionMeta,\n        panelOpen: om.openFindingId === g.id, dirty: this.omRowDirty(g, d),\n        showAmount: (isFull || isPartial), showDate: (isFull || isPartial), showReason: (isPartial || isNotIssued),\n        amountLocked: isFull || isNotIssued,\n        amountDisplay: isFull ? this.fmt(pursuedAmt) : (isNotIssued ? this.fmt(0) : (d.amountEditing ? (d.amount || \"\") : (hasAmt ? this.fmt(parsedAmt) : (d.amount || \"\")))),\n        amountFieldStyle: (isFull || isNotIssued) ? \"background:var(--imp-gray-100); color:var(--imp-fg-muted); cursor:not-allowed;\" : \"\",\n        saveDisabled: !!amtError,\n        autoFullNotice: !!d.autoFullNotice,\n        reasonPlaceholder: isNotIssued ? \"Add details about why the request was declined\" : \"Add details about what was approved\",\n        remainingDisplay: this.fmt(remaining),\n        date: d.date || \"\", reason: d.reason || \"\", error: isPartial ? amtError : (d.error || \"\"),\n      };\n    };\n    const omDisputes = (() => {\n      const om = st.outcomeModal;\n      if (!om) return [];\n      const groups = (st.reviewGroups || []).filter(g => g.pursuit === \"pursued\");\n      const byKey = {};\n      groups.forEach(g => { const k = this.disputeKeyFor(g); (byKey[k] = byKey[k] || []).push(g); });\n      return Object.keys(byKey).map(key => {\n        const dg = byKey[key];\n        const label = this.disputeStatusLabel(dg);\n        const pill = omPillMap[label] || omStatusMeta.awaiting;\n        const amountPursued = dg.reduce((s, g) => s + (g.amountN || 0), 0);\n        let collected = 0;\n        dg.forEach(g => { const d = om.drafts[g.id]; if (!d) return; if (d.status === \"full\") collected += g.amountN || 0; else if (d.status === \"partial\") collected += parseFloat(d.amount || \"0\") || 0; });\n        const bulk = om.bulk[key] ? { status: om.bulk[key].status, reason: om.bulk[key].reason || \"\", count: dg.length, showReason: om.bulk[key].status === \"not_issued\", options: omOptionDefs.map(o => ({ value: o.value, label: o.label, btnStyle: omBtnStyle(omStatusMeta[o.value].accent, om.bulk[key].status === o.value) })) } : null;\n        return {\n          key, threePl: dg[0].threePl || (this.activeMemo() || {}).provider || \"Biller\", sentDate: dg[0].pursuedAt || \"\\u2014\",\n          subtitle: this.fmt(amountPursued) + \" pursued \\u00b7 \" + dg.length + \" finding\" + (dg.length === 1 ? \"\" : \"s\"),\n          statusLabel: label, statusPillBg: pill.pillBg, statusPillFg: pill.pillFg,\n          expanded: !om.collapsed[key], chevronTransform: om.collapsed[key] ? \"none\" : \"rotate(180deg)\",\n          bulk,\n          rows: dg.slice().sort((a, b) => a.id.localeCompare(b.id)).map(g => omRowFor(om, g)),\n          collectedDisplay: this.fmt(collected), remainingDisplay: this.fmt(Math.max(0, amountPursued - collected)),\n        };\n      }).sort((a, b) => this.parseDateSafe(a.sentDate) - this.parseDateSafe(b.sentDate));\n    })();\n    const omSummary = (() => {\n      const om = st.outcomeModal;\n      if (!om) return null;\n      const groups = (st.reviewGroups || []).filter(g => g.pursuit === \"pursued\");\n      const label = this.disputeStatusLabel(groups);\n      const pill = omPillMap[label] || omStatusMeta.awaiting;\n      const amountPursued = groups.reduce((s, g) => s + (g.amountN || 0), 0);\n      let collected = 0;\n      groups.forEach(g => { const d = om.drafts[g.id]; if (!d) return; if (d.status === \"full\") collected += g.amountN || 0; else if (d.status === \"partial\") collected += parseFloat(d.amount || \"0\") || 0; });\n      const disputeCount = omDisputes.length;\n      return {\n        amountPursuedDisplay: this.fmt(amountPursued),\n        disputeCountLabel: disputeCount + \" dispute\" + (disputeCount === 1 ? \"\" : \"s\"),\n        countLabel: groups.length + \" finding\" + (groups.length === 1 ? \"\" : \"s\"),\n        statusLabel: label, statusPillBg: pill.pillBg, statusPillFg: pill.pillFg,\n        collectedDisplay: this.fmt(collected), remainingDisplay: this.fmt(Math.max(0, amountPursued - collected)),\n      };\n    })();\n\n    const dvDisputes = (() => {\n      const dv = st.disputeDetails;\n      if (!dv) return [];\n      const groups = (st.reviewGroups || []).filter(g => g.pursuit === \"pursued\");\n      const byKey = {};\n      groups.forEach(g => { const k = this.disputeKeyFor(g); (byKey[k] = byKey[k] || []).push(g); });\n      return Object.keys(byKey).map(key => {\n        const dg = byKey[key];\n        const label = this.disputeStatusLabel(dg);\n        const pill = omPillMap[label] || omStatusMeta.awaiting;\n        const amountPursued = dg.reduce((s, g) => s + (g.amountN || 0), 0);\n        const collected = dg.reduce((s, g) => s + ((g.collection && (g.collection.status === \"full\" || g.collection.status === \"partial\")) ? (g.collection.amountN || 0) : 0), 0);\n        const rows = dg.slice().sort((a, b) => a.id.localeCompare(b.id)).map(g => {\n          const c = g.collection || {}; const status = c.status || \"awaiting\";\n          const m = omStatusMeta[status] || omStatusMeta.awaiting;\n          const remaining = status === \"partial\" ? Math.max(0, (g.amountN || 0) - (c.amountN || 0)) : 0;\n          return {\n            id: g.id, title: g.title || \"Variance group\", amountPursuedDisplay: this.fmt(g.amountN || 0),\n            statusLabel: m.label, statusIconColor: m.accent, statusPillBg: m.pillBg,\n            isFull: status === \"full\", isPartial: status === \"partial\", isNotIssued: status === \"not_issued\", isAwaiting: status === \"awaiting\",\n            amountDisplay: status === \"full\" ? this.fmt(g.amountN || 0) : (status === \"not_issued\" ? this.fmt(0) : (status === \"partial\" ? this.fmt(c.amountN || 0) : \"\\u2014\")),\n            remainingDisplay: status === \"partial\" ? this.fmt(remaining) : null,\n            date: c.date ? this.fmtDateLong(c.date) : null, reason: c.reason || null,\n          };\n        });\n        return {\n          key, threePl: dg[0].threePl || (this.activeMemo() || {}).provider || \"Biller\", sentDate: dg[0].pursuedAt || \"\\u2014\",\n          subtitle: this.fmt(amountPursued) + \" pursued \\u00b7 \" + dg.length + \" finding\" + (dg.length === 1 ? \"\" : \"s\"),\n          statusLabel: label, statusPillBg: pill.pillBg, statusPillFg: pill.pillFg,\n          expanded: !dv.collapsed[key], chevronTransform: dv.collapsed[key] ? \"none\" : \"rotate(180deg)\",\n          rows, collectedDisplay: this.fmt(collected), remainingDisplay: this.fmt(Math.max(0, amountPursued - collected)),\n        };\n      }).sort((a, b) => this.parseDateSafe(a.sentDate) - this.parseDateSafe(b.sentDate));\n    })();\n    const activityFilterDefs = [\n      { key: \"all\", label: \"All activity\" }, { key: \"dispute\", label: \"Disputes\" },\n      { key: \"outcome\", label: \"Outcome updates\" }, { key: \"report\", label: \"Reports & downloads\" },\n    ];\n    const activityFilters = activityFilterDefs.map(f => { const sel = st.activityFilter === f.key; return { key: f.key, label: f.label, selected: sel, pillStyle: \"border-radius:999px; padding:7px 16px; font:600 12.5px var(--imp-font-body); cursor:pointer; white-space:nowrap; \" + (sel ? \"background:var(--imp-purple-500); color:#fff; border:1.5px solid var(--imp-purple-500);\" : \"background:#fff; color:var(--imp-fg-muted); border:1.5px solid var(--imp-gray-300);\") }; });\n    const activityAll = this.creditMemoActivity();\n    const activityRows = st.activityFilter === \"all\" ? activityAll : activityAll.filter(a => a.cat === st.activityFilter);\n    const activityGroups = (() => {\n      const out = []; let cur = null;\n      activityRows.forEach(a => { if (!cur || cur.dateGroup !== a.dateGroup) { cur = { dateGroup: a.dateGroup, events: [] }; out.push(cur); } cur.events.push({ ...a, isDispute: a.cat === \"dispute\", isOutcome: a.cat === \"outcome\", isReport: a.cat === \"report\", hasChips: !!(a.chips && a.chips.length), hasAction: !!a.actionLabel, iconIsDoc: a.icon === \"doc\", iconIsDownload: a.icon === \"download\", iconIsPaperplane: a.icon === \"paperplane\", iconIsCheck: a.icon === \"check\", iconIsPartial: a.icon === \"partial\", iconIsX: a.icon === \"x\", iconIsClock: a.icon === \"clock\" }); });\n      return out;\n    })();\n\n    const dvSummary = (() => {\n      const dv = st.disputeDetails;\n      if (!dv) return null;\n      const groups = (st.reviewGroups || []).filter(g => g.pursuit === \"pursued\");\n      const label = this.disputeStatusLabel(groups);\n      const pill = omPillMap[label] || omStatusMeta.awaiting;\n      const amountPursued = groups.reduce((s, g) => s + (g.amountN || 0), 0);\n      const collected = groups.reduce((s, g) => s + ((g.collection && (g.collection.status === \"full\" || g.collection.status === \"partial\")) ? (g.collection.amountN || 0) : 0), 0);\n      return {\n        amountPursuedDisplay: this.fmt(amountPursued),\n        disputeCountLabel: dvDisputes.length + \" dispute\" + (dvDisputes.length === 1 ? \"\" : \"s\"),\n        countLabel: groups.length + \" finding\" + (groups.length === 1 ? \"\" : \"s\"),\n        statusLabel: label, statusPillBg: pill.pillBg, statusPillFg: pill.pillFg,\n        collectedDisplay: this.fmt(collected), remainingDisplay: this.fmt(Math.max(0, amountPursued - collected)),\n      };\n    })();\n\n    const lccStatusMap = { ready: [\"Report ready\", okS], partial: [\"Partially available\", procS], preparing: [\"Report preparing\", procS] };\n    const colorMap2 = { purple: \"var(--imp-purple-500)\", amber: \"#B45309\", green: \"var(--imp-success)\", red: \"#B8756D\" };\n    let lccRows = this.lccData();\n    if (st.lccPeriod !== \"all\") lccRows = lccRows.filter(r => this.reportInRange(r.periodKey, st.lccPeriod));\n    const lccView = lccRows.map(r => {\n      const pdfAvail = r.status !== \"preparing\" && !!r.pdfAsset;\n      const xlsxAvail = r.status !== \"preparing\" && !!r.xlsxAsset;\n      const sm = lccStatusMap[r.status] || lccStatusMap.ready;\n      const cta = this.reportLifecycleCta(\"lcc\", r);\n      return {\n        id: r.id, name: r.name, customer: r.customer, period: r.period, version: r.version, dateAvailable: r.dateAvailable,\n        statusLabel: sm[0], statusStyle: sm[1] + \" text-transform:none; letter-spacing:0; font-weight:600;\",\n        packages: r.packages.toLocaleString(\"en-US\"), invoiced: this.fmt(r.invoicedN), savings: this.fmt(r.savingsN), savingsRate: r.savingsRate,\n        pkgAvail: (pdfAvail || xlsxAvail) && !this.dlLocked(), pkgLocked: (pdfAvail || xlsxAvail) && this.dlLocked(), pkgMissing: !(pdfAvail || xlsxAvail),\n        preparing: r.status === \"preparing\", showMetrics: r.status !== \"preparing\",\n        downloaded: st.lccDownloaded.includes(r.id), sample: !!r.sample,\n        ctaColor: colorMap2[cta.color] || colorMap2.purple, ctaStatusLabel: cta.statusLabel, ctaSupporting: cta.supporting, ctaPrimaryLabel: cta.primaryLabel,\n        ctaHandler: cta.primaryKind === \"prep\" ? this.openReportPrep : (cta.primaryKind === \"outcome\" ? this.openReportOutcome : this.openReportDetails),\n      };\n    });\n    let pwvRows = this.pwvData();\n    if (st.pwvPeriod !== \"all\") pwvRows = pwvRows.filter(r => this.reportInRange(r.periodKey, st.pwvPeriod));\n    const pwvView = pwvRows.map(r => {\n      const sm = lccStatusMap[r.status] || lccStatusMap.ready;\n      const avail = r.status !== \"preparing\";\n      const cta = this.reportLifecycleCta(\"pwv\", r);\n      return {\n        id: r.id, name: r.name, period: r.period, dateAvailable: r.dateAvailable, version: r.version,\n        statusLabel: sm[0], statusStyle: sm[1] + \" text-transform:none; letter-spacing:0; font-weight:600;\",\n        packages: r.packages.toLocaleString(\"en-US\"), affected: r.affected.toLocaleString(\"en-US\"),\n        overbilling: this.fmt(r.overbillingN), billed: this.fmt(r.billedN),\n        pkgAvail: avail && !this.dlLocked(), pkgLocked: avail && this.dlLocked(), pkgMissing: !avail,\n        preparing: r.status === \"preparing\", showMetrics: avail,\n        ctaColor: colorMap2[cta.color] || colorMap2.purple, ctaStatusLabel: cta.statusLabel, ctaSupporting: cta.supporting, ctaPrimaryLabel: cta.primaryLabel,\n        ctaHandler: cta.primaryKind === \"prep\" ? this.openReportPrep : (cta.primaryKind === \"outcome\" ? this.openReportOutcome : this.openReportDetails),\n        downloaded: st.pwvDownloaded.includes(r.id),\n      };\n    });\n\n    // ----- fulfillment credit memos (report library) -----\n    const fcmDl = st.fcmDownloaded || [];\n    let fcmRows = this.fcmData();\n    if (st.fcmPeriod && st.fcmPeriod !== \"all\") fcmRows = fcmRows.filter(r => this.reportInRange(r.periodKey, st.fcmPeriod));\n    if (st.fcmStatus && st.fcmStatus !== \"all\") fcmRows = fcmRows.filter(r => st.fcmStatus === \"downloaded\" ? fcmDl.includes(r.id) : r.status === st.fcmStatus);\n    const fcmView = fcmRows.map(r => {\n      const sm = lccStatusMap[r.status] || lccStatusMap.ready;\n      const avail = r.status !== \"preparing\";\n      const dl = fcmDl.includes(r.id);\n      const ev = (st.fcmDlEvents || {})[r.id];\n      const cta = this.reportLifecycleCta(\"fcm\", r);\n      return {\n        id: r.id, displayId: r.displayId || r.id, provider: r.provider || \"Implentio\", cadence: r.cadence || \"Monthly\",\n        name: r.name, period: r.period, dateAvailable: r.dateAvailable, version: r.version,\n        statusLabel: sm[0], statusStyle: sm[1] + \" text-transform:none; letter-spacing:0; font-weight:600;\",\n        orders: r.orders.toLocaleString(\"en-US\"), billed: this.fmt(r.billedN),\n        overbilled: this.fmt(r.overbilledN), overbilledOrders: r.overbilledOrders.toLocaleString(\"en-US\"), avgOverbilling: r.avgOverbilling,\n        pkgAvail: avail && !this.dlLocked(), pkgLocked: avail && this.dlLocked(), pkgMissing: !avail, preparing: r.status === \"preparing\", showMetrics: avail,\n        isNew: !!r.isNew && !dl, isUpdated: r.status === \"updated\", downloaded: dl,\n        downloadedBy: ev ? (\"Downloaded by \" + ev.userFirst + \" \" + ev.userLast + \" on \" + new Date(ev.at).toLocaleDateString(\"en-US\", { month: \"short\", day: \"numeric\", year: \"numeric\" })) : \"\",\n        ctaColor: colorMap2[cta.color] || colorMap2.purple, ctaStatusLabel: cta.statusLabel, ctaSupporting: cta.supporting, ctaPrimaryLabel: cta.primaryLabel,\n        ctaHandler: cta.primaryKind === \"prep\" ? this.openReportPrep : (cta.primaryKind === \"outcome\" ? this.openReportOutcome : this.openReportDetails),\n      };\n    });\n    const fcmSavingsCollectedN = fcmRows.reduce((a, r) => { const rd = this.rdGet(\"fcm\", r.id); if (!rd) return a; return a + (rd.status === \"full\" ? (rd.amountN || 0) : (rd.status === \"partial\" ? (rd.collectedAmountN || 0) : 0)); }, 0);\n    const fcmExec = {\n      savings: this.fmt(fcmRows.reduce((a, r) => a + (r.overbilledN || 0), 0)),\n      count: fcmRows.length,\n      collected: this.fmt(fcmSavingsCollectedN), hasCollected: fcmSavingsCollectedN > 0,\n      memosReady: fcmRows.filter(r => r.status === \"ready\" || r.status === \"updated\").length,\n      reportsDownloaded: fcmDl.length,\n      billed: this.fmt(fcmRows.reduce((a, r) => a + (r.billedN || 0), 0)),\n    };\n    fcmExec.countLabel = fcmExec.count + \" credit memo\" + (fcmExec.count === 1 ? \"\" : \"s\");\n\n    // ----- activity -----\n    const actIcon = { gen: [\"#\", \"var(--imp-purple-500)\", \"var(--imp-purple-100)\"], up: [\"↑\", \"var(--imp-purple-500)\", \"var(--imp-purple-100)\"], view: [\"◎\", \"var(--imp-purple-500)\", \"var(--imp-purple-100)\"], dl: [\"↓\", \"var(--imp-success)\", \"var(--imp-success-bg)\"], send: [\"→\", \"var(--imp-purple-500)\", \"var(--imp-purple-100)\"] };\n    const disputeActs = (st.disputeEvents || []).map(ev => {\n      const isManual = ev.via === \"manual\";\n      const title = (isManual ? \"Dispute package marked as sent to \" : \"Dispute package sent to \") + ev.threePl;\n      const meta = isManual ? (\"Confirmed by \" + ev.by + \" \\u00b7 \" + ev.when) : (\"Sent by \" + ev.by + \" \\u00b7 \" + ev.when + \" \\u00b7 \" + ev.groupCount + \" variance group\" + (ev.groupCount === 1 ? \"\" : \"s\") + \" \\u00b7 \" + ev.amount + \" pursued\");\n      const open = !!(st.openDispute || {})[ev.id];\n      const ic = actIcon.send;\n      return {\n        id: ev.id, isDispute: true, notDispute: false, text: title, meta,\n        iconChar: ic[0], iconStyle: \"width:26px;height:26px;border-radius:999px;flex:none;display:flex;align-items:center;justify-content:center;font:700 13px var(--imp-font-body);color:\" + ic[1] + \";background:\" + ic[2] + \";border:1.5px solid \" + ic[1] + \";\",\n        expanded: open, chevronStyle: \"color:var(--imp-purple-500); transition:transform 120ms;\" + (open ? \" transform:rotate(90deg);\" : \"\"),\n        groups: ev.groups, memoLabel: ev.memoId + \" \\u00b7 \" + ev.version, threePl: ev.threePl, senderEmail: ev.senderEmail, to: ev.to, cc: ev.cc, evidenceFile: ev.evidenceFile, method: ev.method,\n      };\n    });\n    const activity = disputeActs.concat(st.activity.map(a => { const ic = actIcon[a.icon] || actIcon.gen; return { ...a, isDispute: false, notDispute: true, iconChar: ic[0], iconStyle: \"width:26px;height:26px;border-radius:999px;flex:none;display:flex;align-items:center;justify-content:center;font:700 13px var(--imp-font-body);color:\" + ic[1] + \";background:\" + ic[2] + \";border:1.5px solid \" + ic[1] + \";\" }; }));\n\n    // ----- tabs -----\n    const tabDefs = [[\"summary\", \"Summary & Findings\"], [\"invoices\", \"Invoices\"], [\"reports\", \"Activity & exports\"]];\n    const memoTabs = tabDefs.map(([key, label]) => ({ key, label, cls: st.memoTab === key ? \"on\" : \"\" }));\n    const memoTabLabel = (tabDefs.find(t => t[0] === st.memoTab) || tabDefs[0])[1];\n\n    const repBadge = (am0 && !am0.golden)\n      ? (am0.report === \"downloaded\" ? reportPill.downloaded : [\"Report Ready\", okS])\n      : (reportPill[st.reportStatus] || reportPill.ready);\n    const markerStyle = st.notesOn\n      ? \"display:inline-flex;align-items:center;justify-content:center;width:20px;height:20px;border-radius:999px;background:var(--imp-orange-500);color:#fff;border:1.5px solid var(--imp-ink);font:700 11px var(--imp-font-body);cursor:pointer;box-shadow:1px 1px 0 var(--imp-ink);flex:none;padding:0;transition:transform 120ms ease;\"\n      : \"display:none;\";\n    const notesBtnStyle = \"display:inline-flex;align-items:center;gap:8px;border:1.5px solid \" + (st.notesOn ? \"var(--imp-orange-500)\" : \"#4b4680\") + \";background:\" + (st.notesOn ? \"var(--imp-orange-500)\" : \"transparent\") + \";color:#fff;border-radius:7px;padding:5px 11px;font:600 12px var(--imp-font-display);cursor:pointer;\";\n    const disabledBtn = \"opacity:0.4;cursor:not-allowed;box-shadow:none;\";\n\n    let presentMode = false;\n    try {\n      const q = ((location.search || \"\") + (location.hash || \"\")).toLowerCase();\n      presentMode = /[?&#/](present|customer|share|noharness)/.test(q);\n    } catch (e) {}\n    const showHarness = this.props.harness !== \"hidden\" && !presentMode;\n\n    return {\n      showHarness,\n      scenario: st.scenario, setScenario: this.setScenario, reloadScenario: this.reloadScenario,\n      noteCount: this.callouts().length,\n      noteIndex: this.callouts().slice().sort((a, b) => a.n - b.n).filter(c => st.noteScope === \"all\" || c.scope === st.noteScope).map(c => ({ n: c.n, title: c.title, scope: c.scope })),\n      noteScope: st.noteScope, onNoteScope: this.onNoteScope,\n      noteScopeList: [{ value: \"all\", label: \"All note types\" }, ...Array.from(new Set(this.callouts().map(c => c.scope))).map(s => ({ value: s, label: s }))],\n      gotoNote: this.gotoNote,\n      notesBtnStyle, toggleNotes: this.toggleNotes, markerStyle, openNote: this.openNote, closeNote: this.closeNote, activeNote: st.activeNote,\n      navTrackerCls: st.section === \"tracker\" || st.section === \"memo\" ? \"db-nav-item is-active\" : \"db-nav-item\",\n      navParcelCls: st.section === \"tracker\" || st.section === \"memo\" ? \"db-nav-item is-active\" : \"db-nav-item\",\n      navInvoicesCls: st.section === \"invoices\" ? \"db-nav-item is-active\" : \"db-nav-item\",\n      biCarriedScope: st.biCarriedScope || null,\n      hasBiCarriedScope: !!st.biCarriedScope,\n      returnToNetworkBreakdown: this.returnToNetworkBreakdown,\n      biLinkScoped: this.biLinkScoped,\n      toggleRecMeth: this.toggleRecMeth,\n      navLccCls: st.section === \"lcc\" ? \"db-nav-item is-active\" : \"db-nav-item\",\n      ueNotified: !!st.ueNotified, ueNotifyOpen: !st.ueNotified, ueNotifyEmail: \"tori.matthews@implentio.com\",\n      notifyUe: this.notifyUe, unnotifyUe: this.unnotifyUe,\n      navUeCls: st.section === \"ue\" ? \"db-nav-item is-active\" : \"db-nav-item\",\n      navUeStyle: st.section === \"ue\" ? { display: \"flex\", alignItems: \"center\", justifyContent: \"space-between\", gap: \"8px\" } : { display: \"flex\", alignItems: \"center\", justifyContent: \"space-between\", gap: \"8px\", color: \"var(--imp-ink)\", fontWeight: 500 },\n      navPwvCls: st.section === \"pwv\" ? \"db-nav-item is-active\" : \"db-nav-item\",\n      navCollapsed: !!st.navCollapsed, navExpanded: !st.navCollapsed,\n      toggleNav: this.toggleNav,\n      asideStyle: { width: st.navCollapsed ? \"68px\" : \"240px\", flex: \"none\", background: \"var(--imp-white)\", borderRight: \"1.5px solid var(--imp-gray-300)\", display: \"flex\", flexDirection: \"column\", padding: st.navCollapsed ? \"16px 8px\" : \"16px 14px\", position: \"sticky\", top: 0, height: \"100vh\" },\n      curInvoices: st.section === \"invoices\" ? \"page\" : null,\n      curParcel: st.section === \"tracker\" || st.section === \"memo\" ? \"page\" : null,\n      curLcc: st.section === \"lcc\" ? \"page\" : null,\n      curPwv: st.section === \"pwv\" ? \"page\" : null,\n      curUe: st.section === \"ue\" ? \"page\" : null,\n      railInvoicesStyle: { background: st.section === \"invoices\" ? \"var(--imp-purple-100)\" : \"transparent\" },\n      railParcelStyle: { background: st.section === \"tracker\" || st.section === \"memo\" ? \"var(--imp-purple-100)\" : \"transparent\" },\n      railLccStyle: { background: st.section === \"lcc\" ? \"var(--imp-purple-100)\" : \"transparent\" },\n      railPwvStyle: { background: st.section === \"pwv\" ? \"var(--imp-purple-100)\" : \"transparent\" },\n      railUeStyle: { background: st.section === \"ue\" ? \"var(--imp-purple-100)\" : \"transparent\" },\n      navInvoicesIconStyle: { width: \"18px\", height: \"18px\", flex: \"none\", alignSelf: \"flex-start\", marginTop: \"1px\", opacity: 1 },\n      navParcelIconStyle: { width: \"18px\", height: \"18px\", flex: \"none\", alignSelf: \"flex-start\", marginTop: \"1px\", opacity: 1 },\n      navLccIconStyle: { width: \"18px\", height: \"18px\", flex: \"none\", alignSelf: \"flex-start\", marginTop: \"1px\", opacity: 1 },\n      navPwvIconStyle: { width: \"18px\", height: \"18px\", flex: \"none\", alignSelf: \"flex-start\", marginTop: \"1px\", opacity: 1 },\n      navUeIconStyle: { width: \"18px\", height: \"18px\", flex: \"none\", alignSelf: \"flex-start\", marginTop: \"1px\", opacity: 1 },\n      goUe: this.goUe, goPwv: this.goPwv,\n      goTracker: this.goTracker, goLcc: this.goLcc, openMemo: this.openMemo,\n      isTracker: st.section === \"tracker\", isLcc: st.section === \"lcc\", isMemo: st.section === \"memo\", isInvoices: st.section === \"invoices\",\n      goInvoices: this.goInvoices,\n      isUe: st.section === \"ue\", isPwv: st.section === \"pwv\", isAccount: st.section === \"account\",\n      isBi: st.section === \"bi\", isBiCpo: st.section === \"bicpo\",\n      bi: this.biVals(st), biRecordsOpen: !!st.biRecordsOpen,\n      goBi: this.goBi, goBiCpo: this.goBiCpo, openBiDriver: this.openBiDriver, clearBiDriver: this.clearBiDriver,\n      setBiDim: this.setBiDim, drillBiProv: this.drillBiProv, clearBiProv: this.clearBiProv, selBiTrend: this.selBiTrend, openBiRecords: this.openBiRecords, closeBiRecords: this.closeBiRecords,\n      setBiTab: this.setBiTab, exportBiData: this.exportBiData,\n      toggleImpLc: this.toggleImpLc, toggleImpCov: this.toggleImpCov, addImpactToReport: this.addImpactToReport, selImpPeriod: this.selImpPeriod, impReviewLargest: this.impReviewLargest,\n      setNetView: this.setNetView, netDrill: this.netDrill, driverNext: this.driverNext, netRoot: this.netRoot, netUpNode: this.netUpNode, netFromAnomaly: this.netFromAnomaly,\n      toggleNetCoverage: this.toggleNetCoverage, toggleNetCoverageProv: this.toggleNetCoverageProv, netCrumbClick: this.netCrumbClick,\n      setBiWin: this.setBiWin, setBiFocus: this.setBiFocus, clearBiFocus: this.clearBiFocus, setBiMonth: this.setBiMonth,\n      closeTrendInspect: this.closeTrendInspect, viewMonthReport: this.viewMonthReport,\n      setBenchCohort: this.setBenchCohort, toggleBenchFull: this.toggleBenchFull, toggleBenchScoreFull: this.toggleBenchScoreFull, toggleBenchDefs: this.toggleBenchDefs,\n      toggleBiMeth: this.toggleBiMeth,\n      setEventMonth: this.setEventMonth, toggleEventMonthRow: this.toggleEventMonthRow, selEventDetail: this.selEventDetail, closeEventDetail: this.closeEventDetail,\n      setEventFilter: this.setEventFilter, toggleEventLedger: this.toggleEventLedger, toggleEventExport: this.toggleEventExport,\n      setEventExportRange: this.setEventExportRange, toggleEventExportInReport: this.toggleEventExportInReport, runEventExport: this.runEventExport,\n      toggleBiCpoDetail: this.toggleBiCpoDetail, toggleBiSpendOpen: this.toggleBiSpendOpen, toggleBiParcelOpen: this.toggleBiParcelOpen, toggleBiFulfOpen: this.toggleBiFulfOpen,\n      goBiNetwork: this.goBiNetwork, goBiNetworkProv: this.goBiNetworkProv, goBiNetworkCarrier: this.goBiNetworkCarrier, goBiNetworkNode: this.goBiNetworkNode,\n      connectRevenueSource: this.connectRevenueSource, stopProp: this.stopProp, goTracker: this.goTracker,\n      toggleTb: this.toggleTb, closeTb: this.closeTb, tbInterval: this.tbInterval, tbCmp: this.tbCmp,\n      tbFilter: this.tbFilter, clearBiFilters: this.clearBiFilters, tbCustomDate: this.tbCustomDate, applyCustomRange: this.applyCustomRange,\n      setImpactBasis: this.setImpactBasis, toggleImpactCalc: this.toggleImpactCalc, toggleTimeline: this.toggleTimeline,\n      openBiScope: this.openBiScope, cancelBiScope: this.cancelBiScope, applyBiScope: this.applyBiScope, resetPendingScope: this.resetPendingScope,\n      toggleAboutPreview: this.toggleAboutPreview, closeAboutPreview: this.closeAboutPreview, onAboutPreviewKey: this.onAboutPreviewKey,\n      toggleScopeGrp: this.toggleScopeGrp, toggleDd: this.toggleDd, pickDd: this.pickDd,\n      matchDashboard: this.matchDashboard, keepAsstScope: this.keepAsstScope, toggleAskMore: this.toggleAskMore,\n      openBiReport: this.openBiReport, closeBiReport: this.closeBiReport, biReportOpen: !!st.biReportOpen,\n      exportBiDetail: this.exportBiDetail, goBiRecon: this.goBiRecon, goBiOpt: this.goBiOpt,\n      askBi: this.askBi, onBiAskText: this.onBiAskText, submitBiAsk: this.submitBiAsk, onBiAskKey: this.onBiAskKey,\n      clearBiAsk: this.clearBiAsk, biLink: this.biLink,\n      setBiScopeOpt: this.setBiScopeOpt, resetBiScope: this.resetBiScope,\n      askNext: this.askNext, applyProposal: this.applyProposal, keepScope: this.keepScope,\n      restoreAns: this.restoreAns, toggleBiEvid: this.toggleBiEvid,\n      biAsstOpen: !!st.biAsstOpen, biAsstClosed: !st.biAsstOpen,\n      openBiAsst: this.openBiAsst, closeBiAsst: this.closeBiAsst, toggleBiAsst: this.toggleBiAsst, biGo: this.biGo,\n      drawerDocked: this.drawerVals(st).drawerDocked,\n      drawerOverlay: this.drawerVals(st).drawerOverlay,\n      drawerScrim: this.drawerVals(st).drawerScrim,\n      launcherShown: this.drawerVals(st).launcherShown,\n      drawerStyle: this.drawerVals(st).drawerStyle,\n      workspaceStyle: this.drawerVals(st).workspaceStyle,\n      harnessStyle: this.drawerVals(st).harnessStyle,\n      launcherStyle: this.drawerVals(st).launcherStyle,\n      navBiCls: (st.section === \"bi\" || st.section === \"bicpo\") ? \"db-nav-item is-active\" : \"db-nav-item\",\n      curBi: (st.section === \"bi\" || st.section === \"bicpo\") ? \"page\" : null,\n      railBiStyle: { background: (st.section === \"bi\" || st.section === \"bicpo\") ? \"var(--imp-purple-100)\" : \"transparent\" },\n      navBiIconStyle: { width: \"18px\", height: \"18px\", flex: \"none\", alignSelf: \"flex-start\", marginTop: \"1px\", opacity: 1 },\n      profileMenuOpen: st.profileMenuOpen, toggleProfileMenu: this.toggleProfileMenu, expandNavToProfile: this.expandNavToProfile, stopProp: this.stopProp,\n      goAccount: this.goAccount, logout: this.logout, signIn: this.signIn, loggedOut: st.loggedOut, hideImg: this.hideImg,\n      lccView, lccEmpty: lccRows.length === 0, lccPeriod: st.lccPeriod, onLccPeriod: this.onLccPeriod,\n      lccCountLabel: lccRows.length + \" report\" + (lccRows.length === 1 ? \"\" : \"s\"),\n      toggleLccExplain: this.toggleLccExplain, lccExplainOpen: st.lccExplain, lccExplainLabel: st.lccExplain ? \"Hide explanation\" : \"What is a Least Cost Carrier analysis?\",\n      downloadLccPackage: this.downloadLccPackage, downloadLccReport: this.downloadLccReport,\n      isLccTabReports: (st.lccTrackerTab || \"reports\") === \"reports\", isLccTabOutcomes: st.lccTrackerTab === \"outcomes\", setLccTrackerTab: this.setLccTrackerTab,\n      lccTabReportsStyle: \"background:none; border:none; border-bottom:2.5px solid \" + ((st.lccTrackerTab || \"reports\") === \"reports\" ? \"var(--imp-purple-500)\" : \"transparent\") + \"; color:\" + ((st.lccTrackerTab || \"reports\") === \"reports\" ? \"var(--imp-ink)\" : \"var(--imp-fg-muted)\") + \"; font:600 14px var(--imp-font-body); padding:10px 4px; cursor:pointer; margin-bottom:-1.5px;\",\n      lccTabOutcomesStyle: \"background:none; border:none; border-bottom:2.5px solid \" + (st.lccTrackerTab === \"outcomes\" ? \"var(--imp-purple-500)\" : \"transparent\") + \"; color:\" + (st.lccTrackerTab === \"outcomes\" ? \"var(--imp-ink)\" : \"var(--imp-fg-muted)\") + \"; font:600 14px var(--imp-font-body); padding:10px 4px; cursor:pointer; margin-bottom:-1.5px;\",\n      lccDonut: this.reportDonut(\"lcc\"), lccDonutNotEmpty: !this.reportDonut(\"lcc\").empty,\n      pwvView, pwvEmpty: pwvRows.length === 0, pwvPeriod: st.pwvPeriod, onPwvPeriod: this.onPwvPeriod,\n      pwvCountLabel: pwvRows.length + \" report\" + (pwvRows.length === 1 ? \"\" : \"s\"),\n      togglePwvExplain: this.togglePwvExplain, pwvExplainOpen: st.pwvExplain, pwvExplainLabel: st.pwvExplain ? \"Hide explanation\" : \"What is a Product Weight Validator analysis?\",\n      downloadPwvPackage: this.downloadPwvPackage, downloadPwvReport: this.downloadPwvReport,\n      isPwvTabReports: (st.pwvTrackerTab || \"reports\") === \"reports\", isPwvTabOutcomes: st.pwvTrackerTab === \"outcomes\", setPwvTrackerTab: this.setPwvTrackerTab,\n      pwvTabReportsStyle: \"background:none; border:none; border-bottom:2.5px solid \" + ((st.pwvTrackerTab || \"reports\") === \"reports\" ? \"var(--imp-purple-500)\" : \"transparent\") + \"; color:\" + ((st.pwvTrackerTab || \"reports\") === \"reports\" ? \"var(--imp-ink)\" : \"var(--imp-fg-muted)\") + \"; font:600 14px var(--imp-font-body); padding:10px 4px; cursor:pointer; margin-bottom:-1.5px;\",\n      pwvTabOutcomesStyle: \"background:none; border:none; border-bottom:2.5px solid \" + (st.pwvTrackerTab === \"outcomes\" ? \"var(--imp-purple-500)\" : \"transparent\") + \"; color:\" + (st.pwvTrackerTab === \"outcomes\" ? \"var(--imp-ink)\" : \"var(--imp-fg-muted)\") + \"; font:600 14px var(--imp-font-body); padding:10px 4px; cursor:pointer; margin-bottom:-1.5px;\",\n      pwvDonut: this.reportDonut(\"pwv\"), pwvDonutNotEmpty: !this.reportDonut(\"pwv\").empty,\n      // tracker\n      tPeriod: st.tPeriod, tStatus: st.tStatus, tReport: st.tReport, tCarrier: st.tCarrier, tRange: st.tRange,\n      tProvider: st.tProvider, onTProvider: this.onTProvider,\n      onTPeriod: this.onTPeriod, onTStatus: this.onTStatus, onTReport: this.onTReport, onTCarrier: this.onTCarrier, onTRange: this.onTRange,\n      exec,\n      memoView, memoEmpty: memoView.length === 0, memoCountLabel: memoView.length + \" credit memo\" + (memoView.length === 1 ? \"\" : \"s\"),\n      trackerTab: st.trackerTab || \"memos\", isTrackerMemos: (st.trackerTab || \"memos\") === \"memos\", isTrackerOutcomes: st.trackerTab === \"outcomes\", setTrackerTab: this.setTrackerTab,\n      tabMemosStyle: \"background:none; border:none; border-bottom:2.5px solid \" + ((st.trackerTab || \"memos\") === \"memos\" ? \"var(--imp-purple-500)\" : \"transparent\") + \"; color:\" + ((st.trackerTab || \"memos\") === \"memos\" ? \"var(--imp-ink)\" : \"var(--imp-fg-muted)\") + \"; font:600 14px var(--imp-font-body); padding:10px 4px; cursor:pointer; margin-bottom:-1.5px;\",\n      tabOutcomesStyle: \"background:none; border:none; border-bottom:2.5px solid \" + (st.trackerTab === \"outcomes\" ? \"var(--imp-purple-500)\" : \"transparent\") + \"; color:\" + (st.trackerTab === \"outcomes\" ? \"var(--imp-ink)\" : \"var(--imp-fg-muted)\") + \"; font:600 14px var(--imp-font-body); padding:10px 4px; cursor:pointer; margin-bottom:-1.5px;\",\n      oc: ocm, ocTable, oc3pl, ocHasFilters, donut, activeSlice, donutNotEmpty: !donut.empty,\n      ocIdentified: this.fmt(identifiedN), ocEligible: this.fmt(disp.eligible), ocAwaiting: this.fmt(disp.awaiting), ocCollected: this.fmt(disp.collected), ocDenied: this.fmt(disp.denied),\n      ocMetricStyleAll: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.ocDisposition === \"all\", \"var(--imp-ink)\"),\n      ocMetricNumAll: this.segNumColor(st.ocDisposition === \"all\", \"var(--imp-ink)\"),\n      ocDispositionAllPressed: st.ocDisposition === \"all\",\n      ocMetricStyleEligible: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.ocDisposition === \"eligible\", \"var(--imp-purple-500)\"),\n      ocMetricNumEligible: this.segNumColor(st.ocDisposition === \"eligible\", \"var(--imp-purple-500)\"),\n      ocDispositionEligiblePressed: st.ocDisposition === \"eligible\",\n      ocMetricStyleAwaiting: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.ocDisposition === \"awaiting\", \"var(--imp-orange-500)\"),\n      ocMetricNumAwaiting: this.segNumColor(st.ocDisposition === \"awaiting\", \"var(--imp-orange-500)\"),\n      ocDispositionAwaitingPressed: st.ocDisposition === \"awaiting\",\n      ocMetricStyleCollected: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.ocDisposition === \"collected\", \"var(--imp-success)\"),\n      ocMetricNumCollected: this.segNumColor(st.ocDisposition === \"collected\", \"var(--imp-success)\"),\n      ocDispositionCollectedPressed: st.ocDisposition === \"collected\",\n      ocMetricStyleDenied: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.ocDisposition === \"denied\", \"#B8756D\"),\n      ocMetricNumDenied: this.segNumColor(st.ocDisposition === \"denied\", \"#B8756D\"),\n      ocDispositionDeniedPressed: st.ocDisposition === \"denied\",\n      onSliceEnter: this.onSliceEnter, onSliceLeave: this.onSliceLeave, onSliceClick: this.onSliceClick, toggleOcNotes: this.toggleOcNotes,\n      ocFilters: [\n        { key: \"ocPeriod\", label: \"Credit memo\", value: st.ocPeriod, allLabel: \"All credit memos\", options: ocMemoOpts, fieldStyle: st.ocPeriod !== \"all\" ? activeFieldStyle : inactiveFieldStyle },\n        { key: \"ocProvider\", label: \"Biller\", value: st.ocProvider, allLabel: \"All Billers\", options: ocProvOpts, fieldStyle: st.ocProvider !== \"all\" ? activeFieldStyle : inactiveFieldStyle },\n        { key: \"ocGroup\", label: \"Variance group\", value: st.ocGroup, allLabel: \"All variance groups\", options: ocGroupOpts, fieldStyle: st.ocGroup !== \"all\" ? activeFieldStyle : inactiveFieldStyle },\n        { key: \"ocPursuit\", label: \"Pursuit status\", value: st.ocDisposition === \"eligible\" ? \"eligible\" : (st.ocDisposition !== \"all\" ? \"pursued\" : st.ocPursuit), allLabel: \"All pursuit statuses\", options: ocPursuitOpts, fieldStyle: (st.ocPursuit !== \"all\" || st.ocDisposition !== \"all\") ? activeFieldStyle : inactiveFieldStyle },\n        { key: \"ocOutcome\", label: \"Collection outcome\", value: st.ocDisposition === \"denied\" ? \"not_issued\" : st.ocOutcome, allLabel: \"All collection outcomes\", options: ocOutcomeOpts, fieldStyle: (st.ocOutcome !== \"all\" || (st.ocDisposition !== \"all\" && st.ocDisposition !== \"eligible\")) ? activeFieldStyle : inactiveFieldStyle },\n      ],\n      ocSummaryLabel,\n      onOcFilter: this.onOcFilter, clearOcFilters: this.clearOcFilters,\n      hasOutcomeModal: !!st.outcomeModal, omSummary, omDisputes, closeOutcomeModal: this.closeOutcomeModal, toggleDispute: this.toggleDispute,\n      hasDisputeDetails: !!st.disputeDetails, dvSummary, dvDisputes, openDisputeDetails: this.openDisputeDetails, closeDisputeDetails: this.closeDisputeDetails, toggleDvDispute: this.toggleDvDispute,\n      hasMemoOutcomeEdit: !!st.memoOutcomeEdit, closeMemoLevelOutcome: this.closeMemoLevelOutcome, onMemoOutcomeStatus: this.onMemoOutcomeStatus, onMemoOutcomeAmount: this.onMemoOutcomeAmount, onMemoOutcomeDate: this.onMemoOutcomeDate, saveMemoLevelOutcome: this.saveMemoLevelOutcome,\n      memoOutcomeAmount: (st.memoOutcomeEdit || {}).amount || \"\", memoOutcomeDate: (st.memoOutcomeEdit || {}).date || \"\", memoOutcomeError: (st.memoOutcomeEdit || {}).error || \"\",\n      memoOutcomeShowAmount: (st.memoOutcomeEdit || {}).status === \"partial\",\n      memoOutcomeOptions: [\"awaiting\", \"full\", \"partial\", \"declined\"].map(v => ({\n        value: v, label: this.memoOutcomeStatusLabel(v),\n        btnStyle: \"display:flex; align-items:center; justify-content:flex-start; gap:8px; padding:10px 12px; border-radius:9px; cursor:pointer; font:600 12.5px var(--imp-font-body); text-align:left; background:\" + (((st.memoOutcomeEdit || {}).status === v) ? \"var(--imp-purple-100)\" : \"#fff\") + \"; border:1.5px solid \" + (((st.memoOutcomeEdit || {}).status === v) ? \"var(--imp-purple-500)\" : \"var(--imp-gray-300)\") + \"; color:var(--imp-ink);\",\n      })),\n      hasMemoDetailsOpen: !!st.memoDetailsOpen, closeMemoLevelDetails: this.closeMemoLevelDetails,\n      memoDisputeAmount: this.fmt((st.memoDispute || {}).amountN || 0),\n      memoDisputeSentAt: (st.memoDispute || {}).sentAt || \"\",\n      memoDisputeStatusLabel: this.memoOutcomeStatusLabel((st.memoDispute || {}).status || \"awaiting\"),\n      memoDisputeStatusStyle: { awaiting: \"background:var(--imp-warning-bg); color:#8a5a05; border-color:var(--imp-warning);\", full: \"background:var(--imp-success-bg); color:var(--imp-success); border-color:var(--imp-success);\", partial: \"background:var(--imp-warning-bg); color:#8a5a05; border-color:var(--imp-warning);\", declined: \"background:#FFF1F0; color:#B42318; border-color:#B42318;\" }[(st.memoDispute || {}).status || \"awaiting\"],\n      memoDisputeHasCollected: !!(st.memoDispute && (st.memoDispute.status === \"full\" || st.memoDispute.status === \"partial\")),\n      memoDisputeCollected: this.fmt(st.memoDispute && st.memoDispute.status === \"full\" ? (st.memoDispute.amountN || 0) : ((st.memoDispute || {}).collectedAmountN || 0)),\n      memoDisputeHasReceived: !!(st.memoDispute && st.memoDispute.receivedDate),\n      memoDisputeReceived: (st.memoDispute && st.memoDispute.receivedDate) ? this.fmtDateLong(st.memoDispute.receivedDate) : \"\",\n      ...(() => {\n        const rp = this.reportPrepVals();\n        const oe = st.reportOutcomeEdit;\n        const statusStyleMap = { awaiting: \"background:var(--imp-warning-bg); color:#8a5a05; border-color:var(--imp-warning);\", full: \"background:var(--imp-success-bg); color:var(--imp-success); border-color:var(--imp-success);\", partial: \"background:var(--imp-warning-bg); color:#8a5a05; border-color:var(--imp-warning);\", denied: \"background:#FFF1F0; color:#B42318; border-color:#B42318;\" };\n        const rdo = st.reportDetailsOpen; const rd = rdo ? this.rdGet(rdo.kind, rdo.id) : null; const drep = rdo ? this.reportById(rdo.kind, rdo.id) : null;\n        const oeRd = oe ? this.rdGet(oe.kind, oe.id) : null; const oeRep = oe ? this.reportById(oe.kind, oe.id) : null; const oeMeta = oeRep ? this.reportMeta(oe.kind, oeRep) : null;\n        const oeCollected = oe ? (oe.status === \"full\" ? (oeRd || {}).amountN || 0 : ((oeRd || {}).status === \"partial\" && oe.status === (oeRd || {}).status ? (oeRd || {}).collectedAmountN || 0 : (oe.status === \"partial\" ? (parseFloat(oe.amount) || 0) : 0))) : 0;\n        return {\n          reportPrep: rp,\n          msgPreviewOpen: st.msgPreviewOpen, msgPreviewLabel: st.msgPreviewOpen ? \"Hide full message\" : \"View full message\", toggleMsgPreview: this.toggleMsgPreview,\n          reportPrepGoConnect: this.reportPrepGoConnect, deferReportPrep: this.deferReportPrep,\n          copyReportPreparedEmail: this.copyReportPreparedEmail, downloadReportPreparedEvidence: this.downloadReportPreparedEvidence, dismissReportManualSent: this.dismissReportManualSent,\n          closeReportPrep: this.closeReportPrep, reportPrepBack: this.reportPrepBack, reportPrepNext: this.reportPrepNext,\n          toggleReportAttachPdf: this.toggleReportAttachPdf, toggleReportAttachXlsx: this.toggleReportAttachXlsx,\n          selectAllReportAttach: this.selectAllReportAttach, clearReportAttach: this.clearReportAttach,\n          onPrepTo: this.onPrepTo, onPrepCc: this.onPrepCc, onPrepSubject: this.onPrepSubject, onPrepIntro: this.onPrepIntro,\n          sendReportPrepared: this.sendReportPrepared,\n          hasReportOutcomeEdit: !!oe, closeReportOutcome: this.closeReportOutcome, onReportOutcomeStatus: this.onReportOutcomeStatus, toggleReportOutcomeEdit: this.toggleReportOutcomeEdit,\n          onReportOutcomeAmount: this.onReportOutcomeAmount, onReportOutcomeDate: this.onReportOutcomeDate, onReportOutcomeReason: this.onReportOutcomeReason, saveReportOutcome: this.saveReportOutcome,\n          reportOutcomeAmount: (oe || {}).amount || \"\", reportOutcomeDate: (oe || {}).date || \"\", reportOutcomeReason: (oe || {}).reason || \"\", reportOutcomeError: (oe || {}).error || \"\",\n          reportOutcomeEditing: !!(oe && oe.editing), reportOutcomeToggleLabel: (oe && oe.editing) ? \"Cancel\" : \"Update outcome\",\n          reportOutcomeShowAmount: (oe || {}).status === \"partial\", reportOutcomeShowDate: !!(oe && oe.status !== \"awaiting\"), reportOutcomeShowReason: !!(oe && oe.status !== \"awaiting\"),\n          reportOutcomeShowFullNote: (oe || {}).status === \"full\",\n          reportOutcomeFullAmount: this.fmt((oeRd || {}).amountN || 0),\n          reportOutcomeLiveUnresolved: (oe || {}).status === \"partial\" ? this.fmt(Math.max(0, ((oeRd || {}).amountN || 0) - (parseFloat((oe || {}).amount) || 0))) : \"\",\n          reportOutcomeModalTitle: oeMeta ? oeMeta.disputeModalTitle : \"Update dispute outcome\",\n          reportOutcomeRecordLabel: oeMeta ? oeMeta.disputeRecordLabel : \"Report dispute\",\n          reportOutcomeBiller: oeMeta ? oeMeta.biller : \"\", reportOutcomeReportName: oeRep ? oeRep.name : \"\", reportOutcomeReportVersion: oeRep ? (oeRep.version || \"Version 1\") : \"\",\n          reportOutcomeSummaryAmount: this.fmt((oeRd || {}).amountN || 0), reportOutcomeSummarySentAt: (oeRd || {}).sentAt || \"\",\n          reportOutcomeSummaryStatusLabel: this.reportOutcomeLabel((oeRd || {}).status || \"awaiting\"), reportOutcomeSummaryStatusStyle: statusStyleMap[(oeRd || {}).status || \"awaiting\"],\n          reportOutcomeSummaryCollected: this.fmt((oeRd || {}).status === \"full\" ? ((oeRd || {}).amountN || 0) : ((oeRd || {}).collectedAmountN || 0)),\n          reportOutcomeSummaryUnresolved: this.fmt(Math.max(0, ((oeRd || {}).amountN || 0) - ((oeRd || {}).status === \"full\" ? ((oeRd || {}).amountN || 0) : ((oeRd || {}).collectedAmountN || 0)))),\n          reportOutcomeOptions: [\"awaiting\", \"full\", \"partial\", \"denied\"].map(v => ({\n            value: v, label: v === \"denied\" ? \"Biller declined\" : this.reportOutcomeLabel(v),\n            btnStyle: \"display:flex; align-items:center; justify-content:flex-start; gap:8px; padding:10px 12px; border-radius:9px; cursor:pointer; font:600 12.5px var(--imp-font-body); text-align:left; background:\" + (((oe || {}).status === v) ? \"var(--imp-purple-100)\" : \"#fff\") + \"; border:1.5px solid \" + (((oe || {}).status === v) ? \"var(--imp-purple-500)\" : \"var(--imp-gray-300)\") + \"; color:var(--imp-ink);\",\n          })),\n          hasReportDetailsOpen: !!rdo, closeReportDetails: this.closeReportDetails,\n          reportDetailsRecordLabel: drep ? this.reportMeta(rdo.kind, drep).disputeRecordLabel : \"Report dispute\",\n          reportDetailsName: drep ? drep.name : \"\", reportDetailsVersion: drep ? (drep.version || \"Version 1\") : \"\", reportDetailsBiller: drep ? this.reportMeta(rdo.kind, drep).biller : \"\",\n          reportDetailsAmount: this.fmt((rd || {}).amountN || 0), reportDetailsSummaryLine: this.fmt((rd || {}).amountN || 0) + \" pursued in one dispute\",\n          reportDetailsSentAt: (rd || {}).sentAt || \"\", reportDetailsTo: (rd || {}).to || \"\", reportDetailsCc: (rd || {}).cc || \"\",\n          reportDetailsAttachments: ((rd || {}).attachments || []).join(\", \"),\n          reportDetailsStatusLabel: this.reportOutcomeLabel((rd || {}).status || \"awaiting\"),\n          reportDetailsStatusStyle: statusStyleMap[(rd || {}).status || \"awaiting\"],\n          reportDetailsHasCollected: !!(rd && (rd.status === \"full\" || rd.status === \"partial\")),\n          reportDetailsCollected: this.fmt(rd && rd.status === \"full\" ? (rd.amountN || 0) : ((rd || {}).collectedAmountN || 0)),\n          reportDetailsUnresolved: this.fmt(Math.max(0, (rd || {}).amountN - (rd && rd.status === \"full\" ? (rd.amountN || 0) : ((rd || {}).collectedAmountN || 0)))),\n          reportDetailsHasReceived: !!(rd && rd.receivedDate),\n          reportDetailsReceived: (rd && rd.receivedDate) ? this.fmtDateLong(rd.receivedDate) : \"\",\n          reportDetailsHasReason: !!(rd && rd.reason),\n          reportDetailsReason: (rd || {}).reason || \"\",\n        };\n      })(),\n      activityFilters, activityGroups, activityEmpty: activityRows.length === 0, setActivityFilter: this.setActivityFilter,\n      toggleOmDropdown: this.toggleOmDropdown, closeOmDropdown: this.closeOmDropdown, selectOmStatus: this.selectOmStatus,\n      toggleFindingRow: this.toggleFindingRow, cancelFinding: this.cancelFinding, saveFinding: this.saveFinding,\n      onOmAmount: this.onOmAmount, onOmAmountFocus: this.onOmAmountFocus, onOmAmountBlur: this.onOmAmountBlur, onOmDate: this.onOmDate, onOmReason: this.onOmReason,\n      openBulk: this.openBulk, cancelBulk: this.cancelBulk, chooseBulkStatus: this.chooseBulkStatus, onBulkReason: this.onBulkReason, confirmBulk: this.confirmBulk,\n      confirmMarkPursued: this.confirmMarkPursued, dismissMarkPursued: this.dismissMarkPursued,\n      memoRollup: this.memoRollupStatus(st.reviewGroups),\n      prepUpdateBanner: st.prepUpdateBanner || null, includeInAnotherRequest: this.includeInAnotherRequest,\n      stageFilter: this.stageFilter, applyFilters: this.applyFilters, resetStagedFilters: this.resetStagedFilters, clearFilterKey: this.clearFilterKey,\n      closeFilterPanelBtn: () => this.closeFilterPanel(true),\n      creditsRealizedAmount: st.creditsRealized != null ? this.fmt(st.creditsRealized) : null,\n      hasCreditsRealized: st.creditsRealized != null,\n      noCreditsRealized: st.creditsRealized == null,\n      ...this.filterPanelVals(\"tracker\", memoView.length + \" credit memo\" + (memoView.length === 1 ? \"\" : \"s\")),\n      ...this.filterPanelVals(\"lcc\", lccRows.length + \" report\" + (lccRows.length === 1 ? \"\" : \"s\")),\n      ...this.filterPanelVals(\"pwv\", pwvRows.length + \" report\" + (pwvRows.length === 1 ? \"\" : \"s\")),\n      ...this.filterPanelVals(\"fcm\", fcmRows.length + \" report\" + (fcmRows.length === 1 ? \"\" : \"s\")),\n      isFcm: st.section === \"fcm\", fcmView, fcmEmpty: fcmRows.length === 0, goFcm: this.goFcm,\n      fcmExec, isFcmTabMemos: (st.fcmTrackerTab || \"memos\") === \"memos\", isFcmTabOutcomes: st.fcmTrackerTab === \"outcomes\", setFcmTrackerTab: this.setFcmTrackerTab,\n      fcmDonut: this.reportDonut(\"fcm\"), fcmDonutNotEmpty: !this.reportDonut(\"fcm\").empty,\n      fcmTabMemosStyle: \"background:none; border:none; border-bottom:2.5px solid \" + ((st.fcmTrackerTab || \"memos\") === \"memos\" ? \"var(--imp-purple-500)\" : \"transparent\") + \"; color:\" + ((st.fcmTrackerTab || \"memos\") === \"memos\" ? \"var(--imp-ink)\" : \"var(--imp-fg-muted)\") + \"; font:600 14px var(--imp-font-body); padding:10px 4px; cursor:pointer; margin-bottom:-1.5px;\",\n      fcmTabOutcomesStyle: \"background:none; border:none; border-bottom:2.5px solid \" + (st.fcmTrackerTab === \"outcomes\" ? \"var(--imp-purple-500)\" : \"transparent\") + \"; color:\" + (st.fcmTrackerTab === \"outcomes\" ? \"var(--imp-ink)\" : \"var(--imp-fg-muted)\") + \"; font:600 14px var(--imp-font-body); padding:10px 4px; cursor:pointer; margin-bottom:-1.5px;\",\n      downloadFcmPackage: this.downloadFcmPackage, downloadFcmReport: this.downloadFcmReport,\n      toggleFcmExplain: this.toggleFcmExplain, fcmExplainOpen: !!st.fcmExplain, fcmExplainLabel: st.fcmExplain ? \"Hide explanation\" : \"What is a Fulfillment Credit Memo?\",\n      navFcmCls: st.section === \"fcm\" ? \"db-nav-item is-active\" : \"db-nav-item\",\n      navFcmIconStyle: { width: \"18px\", height: \"18px\", flex: \"none\", alignSelf: \"flex-start\", marginTop: \"1px\", opacity: 1 },\n      curFcm: st.section === \"fcm\" ? \"page\" : null,\n      railFcmStyle: { background: st.section === \"fcm\" ? \"var(--imp-purple-100)\" : \"transparent\" },\n      // memo\n      activeMemo, memoTabs, memoTabLabel, setMemoTab: this.setMemoTab,\n      disputeStatus: this.memoDisputeStatus() || {}, hasDisputeStatus: !!this.memoDisputeStatus(), openMemoOutcome: this.openMemoOutcome,\n      nextStep: (am0 && am0.golden) ? this.nextStepCard() : null,\n      recovery: (am0 && am0.golden) ? this.recoveryStatus() : null,\n\n      mSummaryProcessing: st.memoTab === \"summary\" && st.auditProcessing,\n      mSummaryNormal: st.memoTab === \"summary\" && !st.auditProcessing,\n      mInvoices: st.memoTab === \"invoices\", mOver: st.memoTab === \"overcharge\", mUnder: st.memoTab === \"undercharge\",\n      mReports: st.memoTab === \"reports\",\n      partial: st.partial,\n      reportBadgeLabel: repBadge[0], reportBadgeStyle: repBadge[1],\n      // invoices\n      invCarrier: st.invCarrier, invResult: st.invResult, invSearch: st.invSearch,\n      onInvCarrier: this.onInvCarrier, onInvResult: this.onInvResult, onInvSearch: this.onInvSearch,\n      invoiceView, invoiceEmpty: filtered.length === 0, invStats,\n      allChecked, toggleAll: this.toggleAll, toggleRow: this.toggleRow,\n      noSelection: selCount === 0,\n      downloadSelStyle: selCount === 0 ? disabledBtn : \"\",\n      downloadSelected: this.downloadSelected, downloadAllInvoices: this.downloadAllInvoices,\n      viewInvoice: this.viewInvoice, closeInvoice: this.closeInvoice,\n      activeInvoice: (() => { const ai = st.activeInvoice; if (!ai) return null; const all = ai.orders || []; const shown = all.slice(0, 60).map(o => ({ so: o.so, tracking: o.track, month: o.month, carrier: o.carrier, service: o.service, wh: o.wh, zip: o.zip, labelDate: this.xdate(o.label), zones: (o.az || \"—\") + \" / \" + (o.ez || \"—\"), weight: o.wt + \" oz\", invoiced: this.fmt(o.invN), expected: this.fmt(o.expN), variance: this.fmt(o.varN), outcome: o.outcome })); return { inv: ai.inv, resultLabel: ai.resultLabel || \"\", resultStyle: resultStyleMap[ai.kind] || \"\", invoiced: this.fmt(ai.invoicedN), expected: ai.expectedN == null ? dash : this.fmt(ai.expectedN), variance: ai.varN == null ? dash : (ai.varN === 0 ? dash : this.fmt(ai.varN)), carrier: ai.carrier || \"\", warehouse: ai.warehouse || \"\", monthLabel: ai.date || \"\", orderCount: ai.orderCount || all.length, shownCount: shown.length, hasMore: all.length > shown.length, orders: shown }; })(),\n      // findings\n      reviewGroups, reviewEmpty: st.reviewGroups.length === 0 && !(activeMemo && activeMemo.allNoVariance), reviewSuccessEmpty: !!(activeMemo && activeMemo.allNoVariance), hasFindings: st.reviewGroups.length > 0, toggleGroup: this.toggleGroup,\n      setDetail: this.setDetail, toggleExplanation: this.toggleExplanation, toggleRecord: this.toggleRecord, toggleChargeRow: this.toggleChargeRow, viewEvidenceSource: this.viewEvidenceSource,\n      onGroupSearch: this.onGroupSearch, showMoreRecords: this.showMoreRecords, showFewerRecords: this.showFewerRecords, showMorePackages: this.showMorePackages,\n      downloadErrorGroup: this.downloadErrorGroup, openOrderEvidence: this.openOrderEvidence, closeOrderEvidence: this.closeOrderEvidence,\n      orderEvidence: (() => {\n        const oe = st.orderEvidence; if (!oe) return null;\n        const g = st.reviewGroups.find(x => x.id === oe.gid); if (!g) return null;\n        const rec = g.records.find(r => r.id === oe.rid); if (!rec) return null;\n        const o = (rec.orders || [])[oe.oi]; if (!o) return null;\n        const CH = this.chargeDefs();\n        const ratecard = (g.evidence || []).find(v => v.key.indexOf(\"ratecard\") === 0) || {};\n        const totalStyle = \"font-weight:700; border-top:1.5px solid var(--imp-gray-300);\";\n        const tc = (x) => this.tcase(x);\n        const svc = o.sv ? tc(o.sv) : dash;\n        const zone = o.az || o.ez || null;\n        const oz = o.wt;\n        const lb = oz != null ? Math.round((oz / 16) * 10) / 10 : null;\n        const wtText = oz != null ? (lb + \" lb \\u00b7 \" + oz + \" oz\") : dash;\n        const rows = CH.map(c => {\n          const iv = (o[c[0]] || [0, 0])[0], ex = (o[c[0]] || [0, 0])[1];\n          const df = this.r2(iv - ex);\n          return { key: c[0], label: c[2], iv: iv, ex: ex, df: df };\n        });\n        const active = rows.filter(r => Math.abs(r.iv) > 0.005 || Math.abs(r.ex) > 0.005 || Math.abs(r.df) > 0.005);\n        const unf = active.filter(r => r.df > 0.005).sort((a, b) => b.df - a.df);\n        const fav = active.filter(r => r.df < -0.005).sort((a, b) => a.df - b.df);\n        const chargeRows = active.map(r => ({\n          label: r.label,\n          invoiced: this.fmt(r.iv), expected: this.fmt(r.ex),\n          variance: Math.abs(r.df) < 0.005 ? \"\\u2014\" : (r.df > 0 ? this.posMoney(r.df) + \" unfavourable\" : this.posMoney(r.df) + \" favourable\"),\n          labelStyle: \"\", rowStyle: st.fCategory === r.key ? \"background:var(--imp-warning-bg);\" : \"\",\n          varStyle: Math.abs(r.df) < 0.005 ? \"color:var(--imp-fg-muted);\" : (r.df > 0 ? \"color:var(--imp-orange-500); font-weight:700;\" : \"color:var(--imp-success); font-weight:700;\"),\n        }));\n        chargeRows.push({\n          label: \"Package net variance\", invoiced: this.fmt(o.ti), expected: this.fmt(o.te),\n          variance: this.posMoney(o.tv) + \" unfavourable\",\n          labelStyle: totalStyle, varStyle: totalStyle + \"color:var(--imp-orange-500);\", rowStyle: \"background:var(--imp-gray-100);\",\n        });\n        const F = [[\"Carrier\", o.car || dash], [\"Service level\", svc], [\"Ship date\", this.xdate(o.ld)], [\"Warehouse\", o.wh || dash]];\n        if (o.zip) F.push([\"Destination ZIP\", o.zip]);\n        if (zone) F.push([\"Zone\", zone]);\n        if (oz != null) F.push([\"Billed weight\", wtText]);\n        if (o.cu) F.push([\"Cubic pricing\", \"Yes\"]);\n        F.push([\"Variance group\", g.category]);\n        const facts = F.map(f => ({ label: f[0], value: f[1] }));\n        const calcRows = [\n          { label: \"Primary variance driver on this package\", value: unf.length ? (unf[0].label + \" \" + this.posMoney(unf[0].df)) : \"No unfavourable charge difference\" },\n          { label: \"Unfavourable charge differences\", value: unf.length ? unf.map(r => r.label + \" \" + this.posMoney(r.df)).join(\" \\u00b7 \") : dash },\n          { label: \"Favourable differences that offset them\", value: fav.length ? fav.map(r => r.label + \" \" + this.posMoney(r.df)).join(\" \\u00b7 \") : \"None\" },\n          { label: \"Package net variance\", value: this.posMoney(o.tv) },\n        ];\n        const supportText = \"Implentio compares every charge on the package, then nets the differences to one package result. \"\n          + (fav.length ? \"A favourable difference on \" + fav.map(r => r.label.toLowerCase()).join(\" and \") + \" reduces the unfavourable amount rather than being reported as a separate favourable finding. \" : \"\")\n          + \"This package is assigned to \\u201c\" + g.title + \"\\u201d and appears in no other finding.\";\n        const invrec = (g.evidence || []).find(v => v.key === \"invrec\") || {};\n        const sources = [];\n        if (Math.abs((o.f || [0, 0])[0]) > 0.005 || Math.abs((o.f || [0, 0])[1]) > 0.005) sources.push({ kind: \"Carrier publication\", name: (o.car || \"Carrier\") + \" fuel surcharge \\u2014 week of \" + this.xdate(o.ld), available: false, unavailable: true, gid: g.id, key: \"fuel\" });\n        if (Math.abs((o.d || [0, 0])[0]) > 0.005 || Math.abs((o.d || [0, 0])[1]) > 0.005) sources.push({ kind: \"Carrier list\", name: (o.car || \"Carrier\") + \" delivery area ZIP codes \\u2014 effective \" + this.xdate(o.ld), available: false, unavailable: true, gid: g.id, key: \"daslist\" });\n        sources.push({ kind: \"Source invoice\", name: ((window.CM_DATA && window.CM_DATA.memo.biller) || \"QuickBox\") + \" invoice \" + o.inv, available: !!invrec.available, unavailable: !invrec.available, gid: g.id, key: \"invrec\" });\n        return {\n          so: o.so, inv: o.inv, tracking: o.t,\n          facts, chargeRows,\n          hasCalcRows: true, calcTitle: \"How the charge differences net\", calcRows,\n          hasSupport: true, supportLabel: \"Charge differences \\u2192 package net variance\", supportText,\n          sources,\n          inputs: [svc, oz != null ? wtText : null, zone ? \"Zone \" + zone : null, this.xdate(o.ld) + \" ship date\"].filter(Boolean).join(\" \\u00b7 \"),\n          method: \"Every charge on the package was rebuilt from its shipment facts and contracted terms, then the charge-level differences were netted to one package result.\",\n        };\n      })(),\n      fCarrier: st.fCarrier, fService: st.fService, fCategory: st.fCategory, fDisputeStatus: st.fDisputeStatus,\n      onFCarrier: this.onFCarrier, onFService: this.onFService, onFCategory: this.onFCategory, onFDisputeStatus: this.onFDisputeStatus,\n      clearFindingFilters: this.clearFindingFilters,\n      carrierOpts, serviceOpts, chargeOpts, chargeHl: hl !== \"all\", chargeHlLabel: hlLabel,\n      fCarrierStyle: st.fCarrier !== \"all\" ? activeCompactStyle : inactiveCompactStyle,\n      fServiceStyle: st.fService !== \"all\" ? activeCompactStyle : inactiveCompactStyle,\n      fCategoryStyle: st.fCategory !== \"all\" ? activeCompactStyle : inactiveCompactStyle,\n      fDisputeStatusStyle: st.fDisputeStatus !== \"all\" ? activeCompactStyle : inactiveCompactStyle,\n      findingCountLabel: (() => { const n = [st.fCarrier, st.fService, st.fCategory, st.fDisputeStatus].filter(v => v !== \"all\").length; return n + \" active filter\" + (n === 1 ? \"\" : \"s\") + \" \\u00b7 Showing \" + groupsFiltered.length + \" of \" + st.reviewGroups.length + \" findings\"; })(),\n      findingsFiltered: groupsFiltered.length !== st.reviewGroups.length || st.fCategory !== \"all\" || st.fDisputeStatus !== \"all\",\n      toggleFindingsPanel: this.toggleFindingsPanel, clearFindingField: this.clearFindingField, stopProp: this.stopProp,\n      findingsPanelOpen: st.fpOpen === \"findings\",\n      fpExpandedFindings: st.fpOpen === \"findings\" ? \"true\" : \"false\",\n      findingActiveCount: [st.fCarrier, st.fService, st.fCategory, st.fDisputeStatus].filter(v => v !== \"all\").length,\n      fpBtnStyleFindings: [st.fCarrier, st.fService, st.fCategory, st.fDisputeStatus].some(v => v !== \"all\")\n        ? { display: \"inline-flex\", alignItems: \"center\", gap: \"8px\", background: \"var(--imp-purple-100)\", borderColor: \"var(--imp-purple-500)\", color: \"var(--imp-ink)\" }\n        : { display: \"inline-flex\", alignItems: \"center\", gap: \"8px\" },\n      findingChips: [\n        st.fCarrier !== \"all\" ? { key: \"fCarrier\", label: (carrierOpts.filter(o => o.value === st.fCarrier)[0] || {}).label || st.fCarrier } : null,\n        st.fService !== \"all\" ? { key: \"fService\", label: (serviceOpts.filter(o => o.value === st.fService)[0] || {}).label || st.fService } : null,\n        st.fCategory !== \"all\" ? { key: \"fCategory\", label: (chargeOpts.filter(o => o.value === st.fCategory)[0] || {}).label || st.fCategory } : null,\n        st.fDisputeStatus !== \"all\" ? { key: \"fDisputeStatus\", label: { eligible: \"Eligible to pursue\", awaiting_outcome: \"Awaiting outcome\", fully_collected: \"Fully collected\", partly_collected: \"Partly collected\", declined: \"Biller declined\" }[st.fDisputeStatus] || st.fDisputeStatus } : null,\n      ].filter(Boolean),\n      findingsFilteredEmpty: st.reviewGroups.length > 0 && groupsFiltered.length === 0,\n      // ----- invoice index -----\n      ...(() => {\n        const all = this.invoiceIndexData();\n        const statusMap = {\n          variance: { label: \"Variance identified\", style: \"background:#FFE9D6; color:var(--imp-orange-500); border-color:var(--imp-orange-300); text-transform:none; letter-spacing:0; font-weight:600;\" },\n          clear: { label: \"No significant variance\", style: okS + \" text-transform:none; letter-spacing:0; font-weight:600;\" },\n          pending: { label: \"Audit not complete\", style: procS + \" text-transform:none; letter-spacing:0; font-weight:600;\" },\n          historical: { label: \"Historical record\", style: \"background:var(--imp-gray-200); color:var(--imp-fg-muted); border-color:var(--imp-gray-300); text-transform:none; letter-spacing:0; font-weight:600;\" },\n        };\n        const q = st.ivSearch.trim().toLowerCase();\n        const rows = all.filter(r =>\n          (!q || r.inv.toLowerCase().includes(q)) &&\n          (st.ivBiller === \"all\" || r.biller === st.ivBiller) &&\n          (st.ivPeriod === \"all\" || this.reportInRange(r.period, st.ivPeriod)) &&\n          (st.ivResult === \"all\" || (st.ivResult === \"reviewed\" ? (r.status === \"variance\" || r.status === \"clear\") : r.status === st.ivResult)) &&\n          (st.ivCarrier === \"all\" || (r.carriers || []).indexOf(st.ivCarrier) >= 0) &&\n          (st.ivMemo === \"all\" || (st.ivMemo === \"linked\" ? !!r.memoId : !r.memoId)));\n        const sortedRows = st.ivSort ? rows.slice().sort((a, b) => (this.periodSortValue(a.period) - this.periodSortValue(b.period)) * (st.ivSort === \"desc\" ? -1 : 1)) : rows;\n        const opts = (vals) => Array.from(new Set(vals)).map(v => ({ value: v, label: v }));\n        return {\n          ivSearch: st.ivSearch, ivBiller: st.ivBiller, ivPeriod: st.ivPeriod, ivResult: st.ivResult, ivMemo: st.ivMemo, ivCarrier: st.ivCarrier,\n          onIvSearch: this.onIvSearch, onIvBiller: this.onIvBiller, onIvPeriod: this.onIvPeriod, onIvResult: this.onIvResult, onIvMemo: this.onIvMemo, onIvCarrier: this.onIvCarrier,\n          clearIvFilters: this.clearIvFilters, toggleIvRow: this.toggleIvRow, openMemoFromInvoice: this.openMemoFromInvoice,\n          ivBillerOpts: opts(all.map(r => r.biller)), ivPeriodOpts: opts(all.map(r => r.period)),\n          ivCarrierOpts: opts([].concat.apply([], all.map(r => r.carriers))).sort((a, b) => a.value.localeCompare(b.value)),\n          ivFiltered: rows.length !== all.length,\n          ivSortAria: st.ivSort === \"asc\" ? \"ascending\" : st.ivSort === \"desc\" ? \"descending\" : \"none\",\n          ivSortLabel: st.ivSort === \"asc\" ? \"Period sorted oldest to newest, sort newest to oldest\" : st.ivSort === \"desc\" ? \"Period sorted newest to oldest, remove sorting\" : \"Sort period oldest to newest\",\n          ivSortCaretStyle: { flex: \"none\", transition: \"transform 120ms ease\", opacity: st.ivSort ? 1 : 0.45, transform: st.ivSort === \"desc\" ? \"rotate(180deg)\" : \"none\" },\n          toggleIvSort: this.toggleIvSort,\n          ivEmpty: rows.length === 0,\n          ivCountLabel: \"Showing \" + rows.length + \" of \" + all.length + \" invoices\",\n          ...this.filterPanelVals(\"invoices\", \"Showing \" + rows.length + \" of \" + all.length + \" invoices\"),\n          ivStats: {\n            ingested: all.length,\n            audited: all.filter(r => r.status === \"variance\" || r.status === \"clear\").length,\n            variance: all.filter(r => r.status === \"variance\").length,\n            clear: all.filter(r => r.status === \"clear\").length,\n          },\n          onIvMetric: this.onIvMetric,\n          ivMetricAllStyle: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.ivResult === \"all\", \"var(--imp-purple-500)\"),\n          ivMetricAllNumStyle: this.segNumColor(st.ivResult === \"all\", \"var(--imp-purple-500)\"),\n          ivMetricAllPressed: st.ivResult === \"all\",\n          ivMetricVarianceStyle: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.ivResult === \"variance\", \"var(--imp-orange-500)\"),\n          ivMetricVarianceNumStyle: this.segNumColor(st.ivResult === \"variance\", \"var(--imp-orange-500)\"),\n          ivMetricVariancePressed: st.ivResult === \"variance\",\n          ivMetricClearStyle: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.ivResult === \"clear\", \"var(--imp-ink)\"),\n          ivMetricClearNumStyle: this.segNumColor(st.ivResult === \"clear\", \"var(--imp-ink)\"),\n          ivMetricClearPressed: st.ivResult === \"clear\",\n          ivRows: sortedRows.map(r => ({\n            id: r.id, inv: r.inv, period: r.period, reportPeriod: r.reportPeriod || dash, biller: r.biller, carrierText: r.carrierText, warehouse: r.warehouse,\n            amount: this.fmt(r.amountN), packages: r.packages == null ? dash : r.packages.toLocaleString(\"en-US\"),\n            parcelAmount: r.parcelN == null ? dash : this.fmt(r.parcelN),\n            exceedsInvoice: r.parcelN != null && r.amountN != null && r.parcelN > r.amountN,\n            parcelPackages: r.parcelPkgs == null ? dash : r.parcelPkgs.toLocaleString(\"en-US\"),\n            memoLabel: r.memoId ? (r.memoId + \" \\u00b7 \" + (r.memoVersion || \"Version 1\")) : dash,\n            scopeNote: r.parcelN == null\n              ? \"Implentio has not yet reviewed the parcel charges on this invoice. Other charges, including fulfillment, storage, returns, international, or other excluded services, are not part of this parcel review.\"\n              : (\"Implentio reviewed \" + this.fmt(r.parcelN) + \" in parcel charges from this invoice. Other charges, including fulfillment, storage, returns, international, or other excluded services, were not included in this parcel review.\"),\n            contribution: r.affectedN != null ? (this.fmt(r.affectedN) + \" across \" + r.affectedPkgs.toLocaleString(\"en-US\") + \" packages\") : null,\n            hasContribution: r.affectedN != null,\n            statusLabel: statusMap[r.status].label, statusStyle: statusMap[r.status].style,\n            memoId: r.memoId, hasMemo: !!r.memoId, noMemo: !r.memoId,\n            memoNote: r.status === \"variance\" ? \"Variance identified\" : (r.status === \"clear\" ? \"No significant variance identified\" : \"Audit not complete\"),\n            memoPlaceholder: r.status === \"clear\" ? \"Not required\" : dash,\n            ingested: r.ingested, source: r.source,\n            open: st.ivOpen === r.id,\n            detailLabel: st.ivOpen === r.id ? \"Hide details\" : \"View details\",\n            variance: r.status === \"variance\", clear: r.status === \"clear\", pending: r.status === \"pending\",\n            placeholder: !!r.placeholder,\n          })),\n        };\n      })(),\n      ...(() => {\n        const all = this.memoInvoiceRows();\n        const statusMap2 = {\n          variance: { label: \"Variance identified\", style: \"background:#FFE9D6; color:var(--imp-orange-500); border-color:var(--imp-orange-300); text-transform:none; letter-spacing:0; font-weight:600;\" },\n          clear: { label: \"No significant variance\", style: \"background:var(--imp-purple-100); color:var(--imp-purple-500); border-color:var(--imp-purple-300); text-transform:none; letter-spacing:0; font-weight:600;\" },\n        };\n        const q2 = st.miSearch.trim().toLowerCase();\n        const rows2 = all.filter(r =>\n          (!q2 || r.inv.toLowerCase().includes(q2)) &&\n          (st.miCarrier === \"all\" || (r.carriers || []).indexOf(st.miCarrier) >= 0) &&\n          (st.miStatus === \"all\" || r.status === st.miStatus));\n        const sortedRows2 = st.miSort ? rows2.slice().sort((a, b) => (this.periodSortValue(a.period) - this.periodSortValue(b.period)) * (st.miSort === \"desc\" ? -1 : 1)) : rows2;\n        return {\n          miSearch: st.miSearch, onMiSearch: this.onMiSearch,\n          miSortAria: st.miSort === \"asc\" ? \"ascending\" : st.miSort === \"desc\" ? \"descending\" : \"none\",\n          miSortLabel: st.miSort === \"asc\" ? \"Date sorted oldest to newest, sort newest to oldest\" : st.miSort === \"desc\" ? \"Date sorted newest to oldest, remove sorting\" : \"Sort date oldest to newest\",\n          miSortCaretStyle: { flex: \"none\", transition: \"transform 120ms ease\", opacity: st.miSort ? 1 : 0.45, transform: st.miSort === \"desc\" ? \"rotate(180deg)\" : \"none\" },\n          toggleMiSort: this.toggleMiSort,\n          miEmpty: rows2.length === 0,\n          miCountLabel: \"Showing \" + rows2.length + \" of \" + all.length + \" invoices\",\n          ...this.filterPanelVals(\"memoInvoices\", \"Showing \" + rows2.length + \" of \" + all.length + \" invoices\"),\n          miStatsIncluded: all.length, miStatsVariance: all.filter(r => r.status === \"variance\").length, miStatsClear: all.filter(r => r.status === \"clear\").length,\n          onMiMetric: this.onMiMetric,\n          miMetricAllStyle: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.miStatus === \"all\", \"var(--imp-purple-500)\"),\n          miMetricAllNumStyle: this.segNumColor(st.miStatus === \"all\", \"var(--imp-purple-500)\"),\n          miMetricAllPressed: st.miStatus === \"all\",\n          miMetricVarianceStyle: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.miStatus === \"variance\", \"var(--imp-orange-500)\"),\n          miMetricVarianceNumStyle: this.segNumColor(st.miStatus === \"variance\", \"var(--imp-orange-500)\"),\n          miMetricVariancePressed: st.miStatus === \"variance\",\n          miMetricClearStyle: \"background:#fff; padding:16px 18px;\" + this.segTopBorder(st.miStatus === \"clear\", \"var(--imp-ink)\"),\n          miMetricClearNumStyle: this.segNumColor(st.miStatus === \"clear\", \"var(--imp-ink)\"),\n          miMetricClearPressed: st.miStatus === \"clear\",\n          miRows: sortedRows2.map(r => ({\n            id: r.id, inv: r.inv, period: r.period, carrierText: r.carrierText, warehouse: r.warehouse,\n            amount: this.fmt(r.amountN), packages: r.packages == null ? dash : r.packages.toLocaleString(\"en-US\"),\n            parcelAmount: r.parcelN == null ? dash : this.fmt(r.parcelN),\n            statusLabel: statusMap2[r.status].label, statusStyle: statusMap2[r.status].style,\n          })),\n        };\n      })(),\n      memoRollup: (st.reviewGroups || []).map(g => ({\n        anchor: \"finding-\" + g.id, title: g.title,\n        amount: this.fmt(g.varN != null ? g.varN : g.amountN),\n        packages: (g.packages || 0).toLocaleString(\"en-US\"),\n        invoices: (g.invoices || 0).toLocaleString(\"en-US\"),\n      })),\n      scrollToFinding: this.scrollToFinding,\n      underGroups, underEmpty: st.underGroups.length === 0, toggleUnder: this.toggleUnder,\n      verify: this.verifyData(), notesOn: st.notesOn,\n      openMethod: this.openMethod, closeMethod: this.closeMethod, showMethod: !!st.showMethod,\n      // reports\n      reportReady: am0 ? (am0.golden ? st.reportStatus === \"ready\" : true) : st.reportStatus === \"ready\",\n      reportGenerating: !!(am0 && am0.golden) && st.reportStatus === \"generating\",\n      reportFailed: !!(am0 && am0.golden) && st.reportStatus === \"failed\",\n      contactImplentio: this.contactImplentio, downloadPrevVersion: this.downloadPrevVersion, previewPrevVersion: this.previewPrevVersion,\n      openReport: this.openReport, closeReport: this.closeReport, showReportModal: st.showReportModal,\n      downloadExcel: this.downloadExcel, retryReport: this.retryReport, reportMonths: this.reportMonthsData(),\n      downloadMemoExcel: this.downloadMemoExcel,\n      prep: this.prepVals(), openPrep: this.openPrep, closePrep: this.closePrep, prepNext: this.prepNext, prepBack: this.prepBack,\n      prepToggleGroup: this.prepToggleGroup, prepToggleSelectAll: this.prepToggleSelectAll,\n      toggleDisputeInclusion: this.toggleDisputeInclusion,\n      onPrepTo: this.onPrepTo, onPrepCc: this.onPrepCc, onPrepSubject: this.onPrepSubject, onPrepIntro: this.onPrepIntro, onPrepCsmToggle: this.onPrepCsmToggle,\n      copyPreparedEmail: this.copyPreparedEmail, downloadPreparedEvidence: this.downloadPreparedEvidence,\n      teamMembers: this.state.teamMembersData,\n      openInvite: this.openInvite, closeInvite: this.closeInvite, onInviteName: this.onInviteName, onInviteEmail: this.onInviteEmail, sendInvite: this.sendInvite,\n      inviteOpen: this.state.inviteOpen, inviteName: this.state.inviteName, inviteEmail: this.state.inviteEmail, inviteError: this.state.inviteError,\n      openEditMember: this.openEditMember, closeMemberModal: this.closeMemberModal, onMemberField: this.onMemberField, saveMember: this.saveMember,\n      resendMemberInvite: this.resendMemberInvite, resetMemberPassword: this.resetMemberPassword, revokeMemberAccess: this.revokeMemberAccess,\n      memberModalOpen: this.state.memberModalOpen, memberForm: this.state.memberForm, memberError: this.state.memberError, memberNotice: this.state.memberNotice,\n      threePlContacts: this.state.threePlContactsData.map(c => ({\n        id: c.id, biller: c.biller, contact: c.contact, email: c.email, cc: c.cc || \"—\",\n        dispute: c.dispute ? \"Default dispute contact\" : \"—\",\n        status: c.active ? \"Active\" : \"Inactive\",\n        statusStyle: c.active ? { background: \"var(--imp-success-bg)\", color: \"var(--imp-success)\", borderColor: \"var(--imp-success)\" } : { background: \"var(--imp-gray-100)\", color: \"var(--imp-fg-subtle)\", borderColor: \"var(--imp-gray-300)\" },\n      })),\n      openAddContact: this.openAddContact, openEditContact: this.openEditContact, closeContactModal: this.closeContactModal,\n      contactModalOpen: this.state.contactModalOpen, contactEditId: this.state.contactEditId, contactError: this.state.contactError,\n      contactForm: this.state.contactForm, onContactField: this.onContactField, onContactActive: this.onContactActive, onContactDispute: this.onContactDispute, saveContact: this.saveContact,\n      integrations: [\n        { name: \"Shopify\", detail: \"Order and fulfillment data\", state: \"Connected\", tone: { background: \"var(--imp-success-bg)\", color: \"var(--imp-success)\", borderColor: \"var(--imp-success)\" } },\n      ],\n      gmailAcct: this.emailAcctView(\"gmail\", \"Gmail\"), outlookAcct: this.emailAcctView(\"outlook\", \"Outlook\"),\n      connectModal: this.connectModalVals(), openConnect: this.openConnect, closeConnect: this.closeConnect, connectContinue: this.connectContinue, connectDemoOutcome: this.connectDemoOutcome, expireConnection: this.expireConnection,\n      disconnectModal: this.disconnectModalVals(), openDisconnectConfirm: this.openDisconnectConfirm, closeDisconnectConfirm: this.closeDisconnectConfirm, confirmDisconnect: this.confirmDisconnect,\n      prepChooseManual: this.prepChooseManual, prepGoConnect: this.prepGoConnect, sendPrepared: this.sendPrepared,\n      prepZipSimulateFailure: this.prepZipSimulateFailure, prepZipRetry: this.prepZipRetry, prepBackToSelections: this.prepBackToSelections,\n      activity, toggleDisputeEvent: this.toggleDisputeEvent,\n      toast: st.toast,\n    };\n  }\n}\n<\u002Fscript>\n\n\n<\u002Fbody><\u002Fhtml>"